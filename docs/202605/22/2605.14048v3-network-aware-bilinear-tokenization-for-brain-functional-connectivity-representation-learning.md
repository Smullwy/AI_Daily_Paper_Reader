---
title: Network-Aware Bilinear Tokenization for Brain Functional Connectivity Representation Learning
title_zh: 网络感知的双线性标记化用于脑功能连接表征学习
authors: "Leo Milecki, Qingyu Hu, Bahram Jafrasteh, Mert R. Sabuncu, Qingyu Zhao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14048v3"
tags: ["query:fclip"]
score: 6.0
evidence: 从fMRI自监督学习脑功能连接表征
tldr: 针对脑功能连接自监督学习，现有方法忽视大尺度网络模块化。本文提出 NERVE 框架，将功能连接矩阵按网络内/间进行分块，并通过双线性分解嵌入异质块，保留网络身份、降低复杂度。在三个发育队列上，NERVE 在行为与病理预测及跨队列迁移中表现更优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14048v3/fig-001.webp\", \"caption\": \"Fig. 1. Overview of NERVE. A. The functional connectivity (FC) matrix is partitioned into patches defined by pairs of functional brain networks. B. Network-aware Bilinear Tokenization. Each functional network is assigned learnable networkspecific weights at initialization, and patch tokens are computed through structured bilinear interactions between network weights during forward. C. MAE Framework. We apply a standard MAE framework to the proposed network-aware tokens, thereby introducing a functionally informed inductive bias over connectivity structure.\", \"page\": 3, \"index\": 1, \"width\": 725, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.14048v3/fig-002.webp\", \"caption\": \"Table 1. Behavioral prediction performance across developmental cohorts. We report Pearson correlation r±∆ with bootstrap 95% CI half-width ∆ from 10-fold cross-validation. n.: negative values, OOD: Out-of-domain.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.14048v3/fig-003.webp\", \"caption\": \"Table 3. Parcellation and network structure ablation. We report Pearson r on 10-fold cross-validation. For the Permutation baselines, we report mean ± std r over 100 random permutations of region-to-network assignment.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 240}]"
motivation: 现有功能连接令牌化方法无法对齐大脑内在的模块化网络组织。
method: 提出按脑网络分区并利用双线性因子分解嵌入异质连接块的 NERVE 框架。
result: 在多队列行为预测和跨数据集评估中，NERVE 的表征更稳定且可迁移。
conclusion: 融入领域特定的结构先验对功能连接组学自监督学习至关重要。
---

## 摘要
掩码自编码器（MAEs）近期在静息态脑功能连接（FC）的自监督表征学习中展现出潜力。然而，一个基本问题尚未解决：如何对FC矩阵进行标记化，以对齐大规模脑网络的内在模块化组织？现有方法通常采用以区域为中心或基于图的方案，将FC视为结构同质元素，而忽略大规模网络组织。我们提出NERVE（通过双线性标记化的脑功能连接网络感知表征），一种自监督学习框架，通过将FC矩阵划分为网络内和网络间连接块，重新定义了FC标记化。不同于基于图像的MAE中固定大小斑块共享一个标记器，由网络对定义的FC斑块大小各异且对应不同的功能角色。为解决此问题，NERVE通过一种新颖的结构化双线性分解嵌入FC斑块。这种公式化保留网络身份，并将参数复杂度从网络数的二次方降低为线性缩放。我们在三个大规模发育队列（ABCD、PNC和CCNP）上评估NERVE用于行为和精神病学预测。相较于结构无关的MAE变体和基于图的自监督基线，提出的网络感知公式产生更稳定和可迁移的表征，尤其在跨队列评估中。消融研究证实，提出的双线性网络嵌入和解剖学基础的脑区分割对性能至关重要。这些发现强调了将领域特定的结构先验融入功能连接组学自监督学习中的重要性。代码见：https://github.com/leomlck/NERVE。

## 速览
**TLDR**：针对现有脑功能连接自监督学习方法忽视大脑模块化网络结构的问题，本文提出NERVE框架，通过将连接矩阵按功能网络分块并采用双线性因子化进行token化，实现网络感知的表示学习。该方法在三个大规模发育队列上展现出更稳定、可迁移的表征，优于传统方法。 \
**Motivation**：现有FC tokenization方法未考虑大脑的内在网络组织，导致表征缺乏生物学可解释性。 \
**Method**：提出NERVE，将FC矩阵划分为网络内和网络间的异质性块，并通过结构化双线性分解进行嵌入。 \
**Result**：在ABCD、PNC和CCNP队列上，NERVE在行为和心理病理学预测中性能优于其他自监督基线，跨队列迁移性更强。 \
**Conclusion**：融入领域特定的结构先验能显著提升功能连接组学自监督表征的稳定性和可迁移性。

---

## Abstract
Masked autoencoders (MAEs) have recently shown promise for self-supervised representation learning of resting-state brain functional connectivity (FC). However, a fundamental question remains unresolved: how should FC matrices be tokenized to align with the intrinsic modular organization of large-scale brain networks? Existing approaches typically adopt region-centric or graph-based schemes that treat FC as structurally homogeneous elements and overlook the large-scale network brain organization. We introduce NERVE (Network-Aware Representations of Brain Functional Connectivity via Bilinear Tokenization), a self-supervised learning framework that redefines FC tokenization by partitioning FC matrices into patches of intra- and inter-network connectivity blocks. Unlike image-based MAE, where fixed-size patches share a common tokenizer, FC patches defined by network pairs are heterogeneous in size and correspond to distinct functional roles. To resolve this problem, NERVE embeds FC patches through a novel structured bilinear factorization. This formulation preserves network identity and reduces parameter complexity from quadratic to linear scaling in the number of networks. We evaluate NERVE across three large-scale developmental cohorts (ABCD, PNC, and CCNP) for behavior and psychopathology prediction. Compared to structurally agnostic MAE variants and graph-based self-supervised baselines, the proposed network-aware formulation yields more stable and transferable representations, particularly in cross-cohort evaluation. Ablation studies confirm that the proposed bilinear network embedding and anatomically grounded parcellation are critical for performance. These findings highlight the importance of incorporating domain-specific structural priors into self-supervised learning for functional connectomics. Code is available at: https://github.com/leomlck/NERVE.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义  
- **核心问题**：在静息态功能连接（FC）的自监督学习中，如何对 FC 矩阵进行 token 化，以使其对齐大脑固有的模块化网络组织。  
- **研究动机**：现有 MAE 或图自监督方法将 FC 视为结构同质的单元（逐行 masking、节点级 masking），忽略了大规模脑网络（如视觉网络、默认网络等）具有的不同功能角色和异质性连接模式，导致得到的表征缺乏生物学可解释性和跨样本稳定性。  
- **整体含义**：提出一种网络感知的双线性 token 化策略，将脑网络结构作为强先验注入 FC 表征学习，提升行为/精神病理预测的性能和跨队列泛化能力。

### 2. 论文提出的方法论  
- **核心思想**：把 FC 矩阵按脑功能网络划分为“块”（intra- 和 inter-network 连接），每个块由一对网络 $(l, m)$ 定义，作为 MAE 的最小单元。针对不同块大小不同且功能意义不同的问题，设计**双线性 token 化**，通过每个网络学习一个矩阵 $U_l \in \mathbb{R}^{|N_l| \times d_E}$，用 Khatri-Rao 积生成 patch 专属的映射矩阵 $W_{l,m} = U_l \odot U_m$，实现 $t_{l,m} = W_{l,m}^\top \text{vec}(x_{l,m})$。  
- **关键技术细节**：  
  - 使用 Schaefer 17‑network 分区 ($R=400$, $N_n=17$, $N_{patch}=153$)。  
  - 相比“共享线性投影”和“每对网络独立投影”，双线性方法参数复杂度从 $O(R^2 d_E)$ 降至 $O(R d_E)$，保留网络身份且参数高效。  
  - 将 token 化后的序列送入标准 Transformer MAE 框架（4 层 Encoder，1 层 Decoder，mask 率 0.5），重建被 mask 的 FC patch，损失为均方误差。  
- **算法流程**：FC 矩阵 → 按网络对分块 → 双线性 token 嵌入 → mask 部分 token → Encoder → Decoder → 重建被 mask 块 → 预测下游任务（Kernel Ridge Regression）。

### 3. 实验设计  
- **数据集与场景**：三个大规模发育队列——ABCD (N=1,791)、PNC (N=1,416)、CCNP (N=178)。ABCD+PNC 用于 SSL 预训练，CCNP 作为域外样本；下游任务为行为/精神病理评分预测（CBCL 和 RBC）。  
- **Benchmark 方法**：  
  - 结构无关 MAE：BrainMass。  
  - 图自监督方法：GraphMAE、BrainGSLs、GATE。  
  - 有监督 FC 模型：BrainNetCNN、BrainGNN、BrainNetTF。  
- **比较方式**：所有 SSL 方法均使用相同预训练数据，用 10 折交叉验证的 KRR 评估表征质量，报告 Pearson 相关性和 bootstrap 置信区间，并进行配对 bootstrap 检验显著性。

### 4. 资源与算力  
- **GPU**：单张 NVIDIA L40 GPU。  
- **训练细节**：预训练 4,000 epoch，batch size 1,024，AdamW 优化器（学习率 $1\times10^{-2}$，权重衰减 $1\times10^{-2}$），cosine 调度、线性 warm-up 400 epoch，混合精度训练。  
- **其他**：下游 KRR 未提及具体算力，整体训练成本可控。

### 5. 实验数量与充分性  
- **主要实验**：在 3 个数据集上预测 6–9 个行为指标，形成多组对比。  
- **消融实验**：  
  1. Tokenization 方式消融（共享线性、每对独立线性、双线性）。  
  2. 脑区划分结构消融（17‑网络、7‑网络、16×16 固定方形 patch，以及随机置换区域标签）。  
- **充分性**：实验设计合理，覆盖了多种对比方法和结构扰动，检验了网络感知设计的必要性和鲁棒性，结果通过统计检验和置信区间支持，评估相对客观、公平。

### 6. 论文的主要结论与发现  
- NERVE 在绝大多数行为指标上优于结构无关的 MAE 变体和图自监督基线，尤其在跨队列（CCNP）的 Externalizing 症状预测上显著领先 ($r=0.33$)。  
- 双线性 token 化在保留网络身份的同时，有效控制了参数膨胀，带来更稳定和可迁移的表征。  
- 解剖学合理的 17‑网络分区是性能的关键；若破坏网络结构（随机打乱或使用无意义的固定 patch），双线性 token 化的优势减弱甚至消失，说明网络感知学习与真实功能组织紧密耦合。  
- 相比有监督模型，SSL 学到的表征在跨队列场景下更具竞争力。

### 7. 优点  
- **创新性 token 化设计**：将脑网络模块化结构直接编码到 MAE 框架中，用 Khatri‑Rao 积实现网络层级的参数共享与分解，兼顾表达力和效率。  
- **深入的消融与验证**：系统比较了多种 token 化方案和不同分区策略，并通过随机置换检验确立了结构特异性。  
- **严格的评估协议**：采用跨队列评估、bootstrap 置信区间和显著性检验，结果可信度高。  
- **开源代码**：提供了可复现的实现。

### 8. 不足与局限  
- **应用范围受限**：仅在发育队列和精神病理学评分上验证，未测试其他人群（如衰老、疾病）或其他表型（如认知任务、诊断分类）。  
- **静态连接限制**：当前仅处理静态 FC，未能考虑动态功能连接或整合结构连接等多模态信息。  
- **分区依赖性**：需要预先定义脑网络 atlas，效果受 atlas 质量和分辨率影响。  
- **下游任务单一**：仅使用 KRR 作为下游评估器；是否能推广到更复杂的预测模型或生成式任务尚不明确。  
- **解释性不足**：虽然 token 化具有网络感知，但未深入分析学到的网络权重或注意力模式所代表的网络间互动，表征的可解释性仍待挖掘。

（完）
