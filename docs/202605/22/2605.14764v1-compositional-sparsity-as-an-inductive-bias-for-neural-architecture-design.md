---
title: Compositional Sparsity as an Inductive Bias for Neural Architecture Design
title_zh: 组合稀疏性作为神经架构设计的归纳偏置
authors: "Hongyu Lin, Antonio Briola, Yuanrong Wang, Tomaso Aste"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14764v1"
tags: ["query:fclip"]
score: 6.0
evidence: 神经网络架构设计的结构先验
tldr: 本研究针对深度神经网络的高维学习难题，探究组合稀疏性作为结构先验。通过结合信息过滤网络提取稀疏依赖与同调神经网络构建固定稀疏连线，提出可解释的层次化架构设计流程。HNNs参数极稀疏，在合成及真实任务上性能匹配或超过密集模型，且方差低、超参数敏感度小，为高效神经架构设计提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14764v1/fig-004.webp\", \"caption\": \"Figure 1. Pipeline for constructing a HNN from data. Input features X are first used to estimate a dependency matrix and construct a MFCF, yielding a decomposable clique tree. The clique structure induces a hierarchy of cliques, which is lifted into a sparse feed-forward neural architecture where units correspond to cliques of increasing order and connections follow incidence. A readout unit aggregates the resulting representations to produce the final prediction.\", \"page\": 4, \"index\": 4, \"width\": 1017, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2605.14764v1/fig-002.webp\", \"caption\": \"Figure 2. Mean test R2 as a function of the dimension-to-sample ratio p/n for three representative feature dimensions (p ∈ {50, 1000, 5000}). For readability, we plot a subset of baselines here; full results across all methods are reported in Appendix C. Each curve shows the mean test R2 across runs at fixed p/n, with error bars indicating ±SEM.\", \"page\": 7, \"index\": 2, \"width\": 1017, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.14764v1/fig-003.webp\", \"caption\": \"Figure 3. Mean test R2 as a function of the dimension-to-sample ratio p/n for p = 1000. We compare HNN (MARGINAL) with a single-layer dense MLP whose number of trainable parameters is matched to that of the HNN (MARGINAL), together with the HNN (ORACLE). Results are averaged over tasks and random seeds, with error bars indicating ±SEM.\", \"page\": 7, \"index\": 3, \"width\": 492, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.14764v1/fig-001.webp\", \"caption\": \"Table 4. OpenML-CTR23 per-dataset test R2 ranks with model parameter count. For each dataset, models are ranked by mean test R2 averaged over all provided cross-validation folds and repeats. Each cell reports rank (number of trainable parameters), where rank 1 denotes the best-performing model on that dataset. Tree-based models are reported with rank only.\", \"page\": 15, \"index\": 1, \"width\": 842, \"height\": 852}]"
motivation: 探究组合稀疏性如何作为一种归纳偏置，指导神经网络结构设计以克服维度灾难。
method: 利用信息过滤网络从数据中提取低维稀疏依赖结构，再通过同调神经网络将其映射为固定连线的稀疏神经图，实现层次化组合。
result: 同调神经网络在合成和真实数据集上，以极少参数达到或超越密集网络的性能，并且更稳定、对超参数不敏感。
conclusion: 组合稀疏性是有效的结构先验，能够设计出参数高效、可解释且鲁棒的稀疏神经网络，为架构设计提供新方向。
---

## 摘要
识别使深度神经网络（DNNs）能够克服维度诅咒的结构先验是机器学习理论中的一个根本性挑战。现有文献表明，有效的高维学习是由组合稀疏性驱动的，其中目标函数分解为支持在低维变量子集上的组成部分。为了研究这一假设，我们将信息过滤网络（IFNs）与同调神经网络（HNNs）相结合，前者通过约束信息最大化提取稀疏依赖结构，后者将推断的拓扑映射为固定连接模式的稀疏神经图。我们形式化了这一构造背后的设计原则，并提出了一个可解释的流程，其中抽象通过层次化组合产生。HNNs 比标准 DNNs 稀疏数个数量级，并且只需要最少的超参数调整。在具有已知稀疏层次结构的合成任务中，HNNs 恢复了底层的组合结构，并在随着维度增加而密集替代方案性能下降的情况下保持稳定。在广泛的真实世界数据集集合上，HNNs 始终能够匹敌或优于密集基线，同时使用少得多的参数，表现出更低的方差，并对超参数表现出更低的敏感性。

## Abstract
Identifying the structural priors that enable Deep Neural Networks (DNNs) to overcome the curse of dimensionality is a fundamental challenge in machine learning theory. Existing literature suggests that effective high-dimensional learning is driven by compositional sparsity, where target functions decompose into constituents supported on low-dimensional variable subsets. To investigate this hypothesis, we combine Information Filtering Networks (IFNs), which extract sparse dependency structures via constrained information maximisation, with Homological Neural Networks (HNNs), which map the inferred topology into fixed-wiring sparse neural graphs. We formalise the design principles underlying this construction and present an interpretable pipeline in which abstraction emerges through hierarchical composition. HNNs are orders of magnitude sparser than standard DNNs and require only minimal hyperparameter tuning. On synthetic tasks with known sparse hierarchies, HNNs recover the underlying compositional structure and remain stable in regimes where dense alternatives degrade as dimensionality increases. Across a broad suite of real-world datasets, HNNs consistently match or outperform dense baselines while using far fewer parameters, exhibiting lower variance and showing reduced sensitivity to hyperparameters.