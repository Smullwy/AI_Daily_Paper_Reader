---
title: "Platonic Representations in the Human Brain: Unsupervised Recovery of Universal Geometry"
title_zh: 人脑中的柏拉图表示：通用几何的无监督恢复
authors: "Pablo Marcos-Manchón, Rishi Jha, Lluís Fuentemilla"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20496v1"
tags: ["query:fclip"]
score: 9.0
evidence: 从fMRI中无监督恢复跨被试的普适几何
tldr: 该研究受强柏拉图表征假说启发，探索人类大脑中是否存在通用神经几何。利用自然场景数据集fMRI数据，提出自监督编码器从重复刺激中学习个体特异性脑嵌入，并通过无监督正交旋转实现跨被试空间的纯几何对齐，无需配对样本。结果表明，对齐后的共享潜空间提升了跨被试检索性能，证实人类视觉皮层存在近似等距的共享神经几何，表征可通过纯几何变换翻译。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20496v1/fig-004.webp\", \"caption\": \"Figure 1: Method overview. (A) Subject encoder. For each subject, fMRI responses are mapped into a low-dimensional embedding space using voxel reliability weighting, PCA, and multi-view CCA (MCCA), followed by a residual nonlinear refinement trained from repeated stimulus presentations. (B) Pairwise brain-to-brain translation. Independently learned subject embeddings are translated between subject pairs by estimating orthogonal rotations Rs→t from geometry-derived pseudocorrespondences. (C) Shared latent space. Pairwise rotations are synchronized to recover one orthogonal transformation Rs per subject, mapping all subject embeddings into a common space.\", \"page\": 3, \"index\": 4, \"width\": 818, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.20496v1/fig-001.webp\", \"caption\": \"Table 2: Within-subject encoder comparison. Retrieval and RSA across repeated presentations, averaged across subjects and repetition pairs. Mean Rank and R@1 measure instance-level stability; RSA measures geometry preservation across repetitions. Lower is better for Mean Rank (chance = 258); higher is better for R@1 (chance = 0.002) and RSA.\", \"page\": 6, \"index\": 1, \"width\": 819, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.20496v1/fig-003.webp\", \"caption\": \"Figure 2: Pairwise brain-to-brain translation. Performance for each ordered subject pair (s, t). Embeddings from source subject s are mapped into target subject t’s space using the unsupervised orthogonal transformation Rs→t and evaluated on the 515 held-out shared images. Mean Rank (average ± std: 2.56± 1.71, chance = 258) and R@1 (average ± std: 0.78± 0.14, chance = 0.002) measure image-level retrieval after translation. RSA is the Pearson correlation between subjectspecific RDMs. Darker colors indicate better performance.\", \"page\": 7, \"index\": 3, \"width\": 812, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2605.20496v1/fig-002.webp\", \"caption\": \"Table 3: Pairwise brain-to-brain translation baselines. Performance is reported as the average across ordered off-diagonal subject pairs on the 515 held-out shared images. Rows compare notranslation controls, optimal-transport matching, external model-space references, and our unpaired orthogonal translation. Lower is better for Mean Rank (chance = 258); higher is better for R@1 (chance = 0.002) and RSA.\", \"page\": 7, \"index\": 2, \"width\": 819, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.20496v1/fig-005.webp\", \"caption\": \"Figure 3: Shared-space brain-to-brain translation. Retrieval performance after synchronizing pairwise brain-to-brain rotations into a single shared latent space. Each subject is mapped into the common coordinate system using one orthogonal transformation Rs, and retrieval is evaluated across ordered subject pairs on the 515 held-out shared images. Left: Mean Rank, lower is better (average ± std: 2.00± 0.76; chance = 258). Right: R@1, higher is better (average ± std: 0.83± 0.09; chance = 0.002). Diagonal entries are omitted. Darker colors indicate better performance in both panels.\", \"page\": 8, \"index\": 5, \"width\": 806, \"height\": 349}]"
motivation: 探究人类大脑是否存在类似人工神经网络的通用潜空间，实现跨被试的无配对表征翻译。
method: 提出自监督编码器学习个体fMRI嵌入，利用无监督正交旋转对齐不同被试的独立空间。
result: 对齐后的共享空间显著提升跨被试检索性能，显示个体空间兼容于共同坐标系。
conclusion: 人类视觉皮层存在共享神经几何，个体表征近似等距，可通过纯几何变换实现跨被试翻译。
---

## 摘要
强柏拉图表示假说认为，人工神经网络中的表征趋同可以被建设性地利用：嵌入可以通过通用潜在空间跨模型转换，而无需配对数据。我们探究人脑中是否可以恢复类似的几何结构。利用自然场景数据集的功能磁共振成像数据，我们提出了一种自监督编码器，仅通过利用重复刺激呈现从脑数据中学习特定于受试者的嵌入。我们证明，这些独立学习的空间可以使用无监督正交旋转跨受试者转换，无需配对跨受试者样本或中间模型表征。将成对旋转同步到单个共享潜在空间进一步改善了跨受试者检索，表明特定于受试者的空间与一个共同的坐标系相互兼容。这些结果为人类视觉皮层中存在共享的神经几何提供了证据：特定于受试者的功能磁共振成像表征在个体之间近似等距，并可以通过纯几何变换进行转换。

## Abstract
The Strong Platonic Representation Hypothesis suggests that representational convergence in artificial neural networks can be harnessed constructively: embeddings can be translated across models through a universal latent space without paired data. We ask whether an analogous geometry can be recovered across human brains. Using fMRI data from the Natural Scenes Dataset, we propose a self-supervised encoder that learns subject-specific embeddings from brain data alone by exploiting repeated stimulus presentations. We show that these independently learned spaces can be translated across subjects using unsupervised orthogonal rotations, without paired cross-subject samples or intermediate model representations. Synchronizing pairwise rotations into a single shared latent space further improves cross-subject retrieval, indicating that subject-specific spaces are mutually compatible with a common coordinate system. These results provide evidence for a shared neural geometry in the human visual cortex: subject-specific fMRI representations are approximately isometric across individuals and can be translated through purely geometric transformations.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化总结。

### 1. 论文的核心问题与研究动机

本文探讨的核心问题是：**人脑的视觉皮层中是否存在一种通用的、共享的神经几何结构？**

*   **研究背景**：
    *   在人工智能领域，“强柏拉图表示假说”认为，独立训练的不同神经网络会收敛到相似的表示空间，并且这些空间可以通过无监督的几何变换（如正交旋转）进行跨模型转换，无需任何配对数据。
    *   此前的证据主要来自人工神经网络，其在生物大脑中是否成立仍是未解之谜。
*   **研究动机**：
    *   检验“强柏拉图表示假说”是否能拓展到人脑，即**特定于个体的fMRI嵌入空间是否可以通过纯几何变换进行跨个体翻译**。
    *   若此假说成立，将表明人脑视觉皮层存在一个内在的、共享的坐标系统，个体差异仅表现为对该系统的近似等距变换（如旋转）。
    *   验证这一假说将为跨被试神经建模、数据共享和脑机接口等应用提供坚实的理论基础。

### 2. 方法论

本文提出一个三阶段框架，旨在从独立的大脑数据中恢复一个共享的潜在表征空间，核心思想是完全**无监督**且**不依赖于任何外部模型**（如预训练的图像或语言模型）。

**阶段一：学习保留几何结构的个体特异性嵌入**
为每个被试 $s$ 学习一个编码器 $f_s: X^{(s)} \rightarrow Z^{(s)}$，该过程仅利用被试自身的fMRI数据和重复刺激作为自监督信号。
1.  **体素可靠性加权**：计算每个体素在多次重复刺激下的平均相关性 $\gamma_v$，并以此对体素响应进行缩放，降低不可靠体素的影响。
2.  **低维线性投影**：
    *   首先使用主成分分析将加权后的fMRI响应降维。
    *   然后应用**多视角典型相关分析**来提取在多次重复中共享的最大相关信息。最终通过岭回归将多视角投影提炼为单一线性映射 $W^*$，得到线性嵌入 $Z_{\text{lin}} = YW^*$。
3.  **非线性残差精炼**：在冻结的线性投影基础上，增加一个由多层感知机（MLP） $g_\theta$ 和可学习标量 $\alpha$ 构成的非线性分支，得到最终嵌入 $Z = f_s(X) = Z_{\text{lin}} + \alpha g_\theta(Z_{\text{lin}})$。
    *   **损失函数**：
        *   **对比损失**：不同重复之间的嵌入对使用InfoNCE损失。
        *   **余弦拉近损失**：直接最小化不同重复之间的嵌入距离，$L_{\text{pull}} = \dfrac{2}{r(r-1)} \sum_{1 \le i < j \le r} (1 - \text{sim}(Z_i, Z_j))$。

**阶段二：成对的大脑间无监督翻译**
目标是学习一个正交变换 $R_{s \rightarrow t} \in O(d)$，使得 $Z^{(s)}R_{s \rightarrow t} \approx Z^{(t)}$，全程不使用任何跨被试的配对图像。
1.  **伪配对生成**：利用K-means聚类和二次分配求解器，基于聚类中心的全局相似性结构来生成初始的“伪配对”。
2.  **迭代精炼与对称化**：
    *   通过解决正交普鲁克问题初始化 $R^{(0)}_{s \rightarrow t}$。
    *   采用类似迭代最近点（ICP）的方法迭代更新：将部分源嵌入通过当前 $R^{(k)}_{s \rightarrow t}$ 转换后，在目标空间寻找最近邻作为新的伪目标，求解新的普鲁克解，并进行滑动平均更新。
    *   **对称化**：强制 $R_{t \rightarrow s} = R^{\top}_{s \rightarrow t}$，以满足全局同步的需求并提升稳定性。

**阶段三：构建共享潜在空间**
通过**正交同步**算法，将所有成对旋转 $R_{s \rightarrow t}$ 集成为一个统一的全球坐标系。
*   构造一个分块矩阵 $B$，其中 $(s, t)$ 块为 $R_{s \rightarrow t}$。
*   理想情况下，$B$ 可以分解为 $B = UU^{\top}$。通过对 $B$ 进行谱分解，取其前 $d$ 个特征向量组成的矩阵 $U$，其各分块 $U_s$ 即近似为被试 $s$ 到共享空间的变换 $R_s$。
*   将每个 $U_s$ 投影到 $O(d)$ 上，得到最终的 $R_s$。所有被试嵌入通过 $Z^{(s)}_{\text{shared}} = Z^{(s)}R_s$ 映射到共同空间。

### 3. 实验设计

*   **数据集**：自然场景数据集（NSD），包含8名被试观看自然图像的fMRI数据。
    *   **关键划分**：训练（学习编码器和翻译）使用**被试特异性、不共享的图像**；评估则严格在**所有被试都看过的515张共享图像**上进行，确保了公平性。
*   **评估指标**：
    *   **被试内评估**：检验编码器能否从噪音重复中提取稳定的刺激表征。
    *   **跨被试评估**：检验翻译后的表征是否能实现跨被试的个体级图像检索。
    *   **具体指标**：平均排名（MR）、Recall@1、表征相似性分析距离的相关性。
*   **对比方法 (Baselines)**：
    *   **被试内编码器对比**：包括预处理fMRI、PCA、多种多视图自监督方法（如DeepCCA）以及由视觉/语言模型引导的监督学习方法。
    *   **跨被试翻译对比**：包括无对齐、基于最优传输的匹配，以及通过外部模型（视觉/语言）空间作为中介的对齐方法。
    *   **模型-大脑对齐诊断实验**：将神经嵌入与多种人工神经网络（如CLIP, DINOv2）的最后一层进行有监督的线性映射。

### 4. 资源与算力
论文中明确说明了实验的算力需求，总体计算成本不高：
*   **硬件配置**：所有实验在单台工作站上完成，配备1块**NVIDIA A40 GPU （48 GB）** 和一颗AMD Ryzen 9 CPU。
*   **时间开销**：
    *   为单个被试训练线性编码器：在CPU上约需**2分钟**。
    *   训练非线性残差MLP：在GPU上约需**1分钟**。
    *   估计一对被试间的脑-脑翻译旋转：在CPU上约需**5分钟**。

### 5. 实验数量与充分性
论文实验设计全面且充分，涵盖了多个维度：
*   **大量对比实验**：
    *   **被试内编码器**：比较了至少5种不同类别的方法（直接神经法、多视图自监督法、模型引导法），并包含了详尽的消融研究。
    *   **跨被试翻译**：比较了4种不同对齐策略。
    *   **模型-大脑对齐**：测试了4个代表性模型和2种映射方法。
*   **多组实验**：
    *   **8名被试**之间所有**56个有序对被试对**的成对翻译进行了评估，内部有效性极高。
    *   **消融实验**：对编码器的各个组件（可靠性加权、MCCA、非线性分支）和超参数（维度、网络深度）进行了详尽的消融和敏感性分析。
*   **稳定性分析**：对翻译过程因初始化导致的敏感性，进行了多随机种子（10次）的实验，并报告了最佳、平均结果及分布，分析客观。
*   **公平性**：严格分离了**非共享的训练图像**和**共享的评估图像**，确保了无监督翻译实验的严谨性和结论的可靠性。

### 6. 主要结论与发现
1.  **人脑视觉皮层存在共享神经几何**：通过纯几何变换（正交旋转）可以实现无监督的跨被试脑活动翻译，证实了“强柏拉图表示假说”在生物大脑中的有效性。
2.  **个体空间近似等距**：不同被试独立学习到的fMRI表征空间通过正交旋转可以高度对齐，说明这些空间的内部几何结构（距离和角度）是近似相同的，个体差异主要表现为坐标系旋转。
3.  **共享坐标系的存在与优势**：将成对旋转同步到一个单一的共享潜在空间，能进一步提升跨被试检索性能，表明所有个体空间相互兼容于一个共同的内在坐标系统。
4.  **自监督编码器的优势**：基于重复刺激的自监督学习方法，在提取稳定的、个体级的刺激信息方面，优于通过线性回归拟合外部预训练模型表征的方法。

### 7. 优点
*   **理论创新性**：首次将AI领域的“强柏拉图表示假说”成功拓展到神经科学领域，并提供了坚实的实证证据。
*   **方法论纯粹性**：整个框架完全是无监督的，不依赖任何预训练人工智能模型或跨被试的配对数据，仅从大脑信号的内在几何结构出发，验证力度强。
*   **约束的严谨性**：将跨被试变换严格限制为正交变换，排除了对几何结构进行任意扭曲的可能性，为“等距”提供了强有力的检验。全局正交同步进一步增强了结果的可靠性。
*   **实验设计的严格性**：严格分离了非共享（训练）和共享（测试）图像，确保了无监督翻译任务的挑战性和评估的公正性。

### 8. 不足与局限
*   **初始化敏感性**：成对的无监督翻译过程对随机种子初始化敏感，需要多次运行并挑选结果，缺乏完全内部的、可靠的模型选择标准。
*   **数据依赖性**：本研究的结论高度依赖于高质量、多次重复刺激的fMRI数据。该方法在信噪比更低的数据集、更小规模的数据集或更高阶认知任务中是否成立尚不明确。
*   **泛化性验证不足**：实验仅在一个视觉感知数据集（NSD）上进行，未测试其对其他刺激分布（如听觉、语言）或任务范式（如记忆、想象）的普适性。
*   **表征层次单一**：研究主要关注高维视觉皮层的“通用代码”，未探讨这种共享几何是否存在于其他脑区或不同的功能网络中。
*   **隐私伦理风险**：论文作者也指出，实现跨被试的通用神经空间构建，可能引发神经数据隐私泄露的风险。

（完）
