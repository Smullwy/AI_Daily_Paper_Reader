---
title: Extended predictive coding framework as variational free-energy minimisation under exponential-family assumption
title_zh: 基于指数族假设的扩展预测编码框架作为变分自由能最小化
authors: "Asaki Kataoka, Kenji Doya"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30882v1"
tags: ["query:fclip"]
score: 8.0
evidence: 预测编码作为感知的脑编码模型
tldr: 本研究将预测编码框架从高斯假设扩展至指数族分布，以弥补传统模型无法捕捉生物神经网络非线性与异质性等缺陷，证明扩展后仍保持与自由能原理的对应（至第二累积量），并可利用局部可塑性规则训练，从而丰富了自由能原理对感知推理神经动态的解释。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30882v1/fig-001.webp\", \"caption\": \"Figure 1: Schematic illustration of EFD–FEP model derived in Section 3. Representational neurons (left, red balls) with internal states ηql emit firing activities (e.g., binary spike, spike count within time window, or firing rate) x̌l sampled from the EFD posterior ql parameterised by ηql . Sampled activities are transformed into prediction about the lower-layer activitiese (left, black arrow). ηql’s temporal evolution along approximate VFE gradient—derived in Sections 3.2.1 and 3.2.2—is driven by the prediction error fed back up the hierarchy (left, green arrow) and prior signals propagated via lateral and another top-down pathway (left, blue arrows). Neuron-wise mean firing activity Eql [x̌l] after convergence given constant input is characterised by activation function ∇Aq(Total input). Any monotonically increasing and differentiable functions can be the activation function. The left-bottom panel shows examples of correspondence between the activation function and class of the posterior; sigmoidal–Bernoulli/binomial, linear–Gaussian, hyperbolic–exponential, and exponential–Poisson (see Section 3.4.2).\", \"page\": 6, \"index\": 1, \"width\": 824, \"height\": 356}]"
motivation: 传统基于高斯假设的预测编码模型无法捕捉生物神经网络的非线性和异质性等特性，且存在负发放率的不合理假设。
method: 将变分后验和先验的分布假设从高斯推广至指数族分布，建立扩展的预测编码框架。
result: 扩展框架展现出生物神经网络缺失的非线性与异质性等特性，并保持与自由能原理的对应（直至后验的第二累积量）。
conclusion: 通过指数族分布拓展，预测编码能更全面地解释感知推理的神经动态，并可由生物学合理的局部可塑性规则训练。
---

## 摘要
大脑的感觉皮层通过其复杂的神经元网络高效地进行感知推理。这一过程的理论解释之一是自由能原理（FEP），它假设大脑执行变分贝叶斯推理。先驱研究表明，在高斯假设和拉普拉斯近似下，FEP可以与预测编码（PC）假说相对应。然而，在如此有限的高斯体系下基于PC的FEP实现未能捕捉到生物神经网络的若干特性，例如网络内输入输出特性的非线性和异质性，以及负发放率的生物学不合理性。本研究表明，当对变分后验和先验假设更广泛的概率分布类别，即指数分布族（EFD）时，这些缺失的特性在网络中得以展现，并维持了FEP-PC的对应关系，直到后验的第二累积量。我们还表明，所提出的模型可以通过生物学上合理的局部可塑性规则进行训练。我们的结果丰富了FEP关于作为变分推理的感知所涉及的神经动力学的解释力。

## Abstract
The sensory cortices of the brain perform perceptual inference efficiently through their complex networks of neurons. One of the theoretical accounts of this process is the free-energy principle (FEP), which postulates that the brain performs variational Bayesian inference. Pioneering studies have shown that FEP can correspond to the predictive coding (PC) hypothesis under the Gaussian assumption and Laplace approximation. However, PC-based implementations of FEP within such a limited Gaussian regime have failed to capture several properties of biological neural networks, such as nonlinearity and heterogeneity of input--output properties within a network, and the biological implausibility of negative firing rates. This study shows that, when a broader class of probability distributions, namely the exponential family of distributions (EFD), is assumed for the variational posterior and prior, these missing characteristics are exhibited within the network, maintaining the FEP--PC correspondence up to the second cumulant of the posterior. We also show that the proposed model can be trained by biologically plausible local plasticity rules. Our results enrich the explanatory power of FEP regarding neural dynamics involved in perception as variational inference.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
*   **关联方向**：本文属于计算神经科学与理论神经编码模型研究，与读者所关注的“brain decoding”、“neural prior”、“representation alignment”等方向弱相关。
*   **启发与意义**：本文提供了一种从“局部推理规则推导出全局表示对齐”的数学框架，有助于理解大脑如何通过先验（neural prior）塑造表征，为设计更生物可解释的脑解码模型提供理论灵感。
*   **可借鉴点**：框架中的“指数族后验-先验耦合”和“局部可塑性规则”可作为构建具有非线性、异质激活函数的生成式脑编码模型的数学基础。
*   **阅读建议**：若读者意图从理论层面深化对“神经先验”与“表征计算”的理解，建议精读第3章的方法论推导及第3.4节的生物映射；若专注于数据驱动的fMRI解码模型实现，则直接技术迁移价值有限，可仅浏览其核心思想。

## 1. 论文的核心问题与整体含义
*   **研究背景与动机**：
    *   大脑感知推理的主流理论之一为**自由能原理（FEP）**，即大脑执行变分贝叶斯推理以最小化变分自由能（VFE）。
    *   先驱工作表明，在高斯假设与拉普拉斯近似下，FEP可在数学上对应**预测编码（PC）**的神经实现：表征神经元编码信念，误差神经元传递预测误差。
    *   然而，严格的**高斯-PC框架**存在显著生物学不合性：1) 要求线性激活函数，无法反映真实神经元的非线性频率-电流（F-I）曲线；2) 无法解释神经元响应特性的异质性；3) 线性激活允许负发放率，与生物现实矛盾。
*   **核心问题**：该FEP-PC对应关系能否在**超越高斯假设**的更广泛概率分布族下依然成立？
*   **整体含义**：本研究旨在通过将后验与先验分布的假设从高斯族扩展至**指数分布族（EFD）**，构建一个能捕捉生物神经网络非线性、异质性等缺失特性的扩展预测编码模型，同时保持其作为变分自由能最小化的规范性解释。

## 2. 论文提出的方法论
*   **核心思想**：将变分后验 $q(x)$ 和先验 $p(x)$ 建模为**指数分布族（EFD）**，其形式为 $f(x; \eta) = h(x) \exp[\eta^\top T(x) - A(\eta)]$，其中 $A(\eta)$ 为严格凸的对数配分函数。似然函数保留为高斯分布。
*   **关键技术推导（EFD-FEP模型）**：
    *   **自由能分解**：在层级马尔可夫链假设下，总变分自由能 $\mathcal{F}$ 分解为各层 $\mathcal{F}_l$ 之和，每层包含KL散度项（后验vs先验）与对数似然期望项。
    *   **推理动态**：通过对 $\mathcal{F}_l$ 求关于后验自然参数 $\eta_{ql}$ 的梯度（并忽略三阶累积量），得到推理动态的简洁形式。对于**自然梯度下降（NGD）**情形：
        $$\tau_{\text{inference}} \dot{\eta}_{ql} = -\eta_{ql} + \eta_{pl} + W_l^{\text{pred}^\top} \bar{\epsilon}_l$$
        其中 $\eta_{pl}$ 为先验参数，$W_l^{\text{pred}^\top} \bar{\epsilon}_l$ 为加权的自底向上预测误差。该动态表明：神经元内部状态 $\eta$ 的更新由先验吸引力与预测误差驱动线性求和完成。
    *   **发放活动**：神经元发放率（均值参数）$\mu_{ql} = \nabla A_{ql}(\eta_{ql})$ 由内部状态 $\eta_{ql}$ 通过激活函数（即F-I曲线）单调映射得到。由于 $A_{ql}$ 的任意严格凸性，该激活函数可以是任意单调递增可导函数，解释了**生物异质性**。
    *   **学习规则**：通过最小化同一VFE目标，导出突触权重的局部可塑性规则：
        1. 预测连接：$\tau \dot{W}_l^{\text{pred}} = \bar{\epsilon}_l \mu_{ql}^\top - G_{\phi l} W_l^{\text{pred}} G_{ql}(\eta_{ql})$ （含不确定性加权衰减的类Hebb规则）
        2. 先验调节连接（侧向/自上而下）：$\tau \dot{W}_l^{\text{rec/TD}} = (\mu_{ql} - \mu_{pl}) \mu_{\bullet}^\top$ （基于总输入与当前发放率之差的塑性规则，与树突平台电位介导的可塑性对齐）。
*   **模型亚型**：
    *   **A型**（可加性分解）：后验可分解为独立单变量分布（如独立伯努利、对角高斯），Fisher信息矩阵 $G_{ql}$ 为对角阵，生物映射直接。
    *   **B型**（非A型）：后验不可分解（如全协方差高斯、类别分布），实现更复杂，可能依赖侧抑制等环路机制。

## 3. 实验设计
*   本文为**纯理论与数学建模论文**，未设计传统计算实验、数据集、基准测试或与其他模型进行量化对比。
*   文中通过理论推导和数学性质分析证明了模型特性（如推理动态形式、学习规则局部性、激活函数异质性等），并辅以生物可解释性论证。

## 4. 资源与算力
*   本文未涉及任何需要计算资源的实验，因此无GPU型号、算力消耗或训练时长等信息。

## 5. 实验数量与充分性
*   本文不包含实验验证部分。其论证的充分性完全依赖于数学推导的严密性、假设的合理性，以及对神经生物学证据的引用与映射分析。从理论构建的角度看，推导链条完整，对主要结论（如动态方程、学习规则、可解释性）均提供了数学证明或构造性推演。

## 6. 论文的主要结论与发现
*   **FEP-PC对应关系可推广至指数族**：当后验和先验为指数族分布、似然为高斯时，VFE最小化的梯度下降动态（在二阶累积量近似下）仍严格保持预测编码的形式。
*   **模型展现缺失的生物特性**：
    *   **非线性与异质性激活函数**：通过选择不同的对数配分函数 $A_{ql}$，模型可自然地容纳任意单调可导的F-I曲线，解释了神经元的异质性。
    *   **解决负发放问题**：选取正值域内单调、逼近零的非线性激活函数（如指数型），可规范地避免负发放率，无需非规范性的整流。
    *   **局部突触可塑性**：所有学习规则均表示为仅依赖局部信号（前后突触活动）的形式，其中某些规则与顶端树突平台电位、钙信号介导的可塑性机制高度一致。
*   **提出A/B型模型亚型**：指出独立因子化（A型）后验有最直接的神经实现；而耦合的B型后验则可能需要更复杂的神经回路机制（如侧抑制）来实现。

## 7. 优点
*   **理论拓展性强**：成功将预测编码的理论基础从狭窄的高斯域扩展至涵盖伯努利、泊松、类别分布等的指数族，极大地增强了该理论的描述能力和生物学范围。
*   **数学-生物映射清晰**：对模型的每个数学变量（内部状态 $\eta$、发放活动 $\mu / \check{x}$、预测误差 $\epsilon$）和每个操作（激活动态、学习规则）都提出了明确、合理的神经生物学对应物。
*   **解决关键生物学矛盾**：直接针对并解决了传统PC模型中的线性、同质性和负发放率三大生物学不合理性，论证过程自洽、规范。

## 8. 不足与局限
*   **缺乏实证验证**：作为纯理论工作，未通过任何模拟实验或数据分析来验证所提出的推理动态和学习规则的有效性与效率，其实际性能尚待检验。
*   **三阶累积量忽略的误差**：推导PC形式的核心一步是忽略了后验的三阶累积量，附录虽讨论了该误差，但在后验非高斯性较强（如泊松）或似然不确定性大时，该近似导致的稳态误差及对自由能下降的保证性未被量化分析。
*   **B型模型实现模糊**：对B型模型（如全协方差高斯）的神经实现方案仅提出“可能依赖侧抑制”等猜测，并未给出具体、可行的回路设计方案。
*   **误差神经元的特殊假设**：模型要求误差神经元具有线性/模拟信号传输、1对1硬连接、突触对称性等理想化特性，这些本身也构成生物学上的挑战（尽管文中讨论了分裂为兴奋/抑制群和反馈对齐等潜在解决方案）。
*   **仅放松后验与先验**：似然函数仍被限制为固定方差的高斯分布，附录B虽讨论了进一步放松至非高斯似然的困难（需瞬间全局信息），但未提出解决方案。

（完）
