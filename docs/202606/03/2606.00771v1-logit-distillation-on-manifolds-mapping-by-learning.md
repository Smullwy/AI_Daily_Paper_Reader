---
title: "Logit Distillation on Manifolds: Mapping by Learning"
title_zh: 流形上的Logit蒸馏：通过学习的映射
authors: "Yiru Yang, Junling Wang, Nishant Kumar Singh, Luohong Wu, Haoran Yan"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00771v1"
tags: ["query:fclip"]
score: 6.0
evidence: 使用投影映射的表征对齐方法
tldr: "为降低模型集成部署的高计算成本，该论文提出一种基于流形的logit蒸馏方法，通过可学习的层和点级投影映射，将学生与教师模型表征对齐到高维空间，并结合LoRA技术，使学生可训练参数降至教师模型的1%以下，同时显著提升词错误率，且训练高效并行。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00771v1/fig-003.webp\", \"caption\": \"Figure 1: Transformation of loss landscape geometry. Standard optimisation assumes a flat Euclidean space (left). By defining the metric tensor gϕ and minimising distances under the induced geometry, optimisation moves into non-Euclidean spaces. The resulting trajectories are illustrated for Riemannian (center) and hyperbolic (right) geometries, showing how learned geometric mappings reshape optimisation paths and improve stability.\", \"page\": 4, \"index\": 3, \"width\": 732, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2606.00771v1/fig-001.webp\", \"caption\": \"Figure 2: Geometry-aware distillation. Teacher and student representations are projected into a shared manifold space, where alignment is performed through geodesic optimization while KL divergence preserves output consistency.\", \"page\": 6, \"index\": 1, \"width\": 762, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2606.00771v1/fig-002.webp\", \"caption\": \"Table 2: Evaluation metric definitions and reporting conventions used throughout Sec. 4. “Direction” indicates whether higher (↑) or lower (↓) values are preferred. WER and CER are reported in percentage points; RTFx is unitless; parameter and memory counts are in MB.\", \"page\": 8, \"index\": 2, \"width\": 648, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2606.00771v1/fig-004.webp\", \"caption\": \"Table 3: Comparison with Whisper baselines and Distil-Whisper on the LibriSpeech test splits. “Train data” is the audio duration used for distillation training. Lower WER and higher RTFx are better indications. Numbers for Whisper and Distil-Whisper baselines are taken from [Radford et al., 2023] and [Gandhi et al., 2023]. RC results are obtained on a single H200 GPU under the protocol of Sec. 4.2.\", \"page\": 9, \"index\": 4, \"width\": 794, \"height\": 256}]"
motivation: 模型集成预测计算开销过大，难以部署到大规模用户。
method: 引入层和点级投影映射，在训练中对齐学生和教师表征于同一高维嵌入空间，并采用LoRA注入大幅减少可训练参数。
result: "学生可训练参数低于教师模型的1%，词错误率相比其他蒸馏方法显著改善。"
conclusion: 所提方法训练快速且可并行，不同于专家混合模型，在参数效率和性能间取得良好平衡。
---

## 摘要
提升几乎任何机器学习模型性能的一种简单方法是，不训练单个模型，而是训练多个采用不同算法的模型，它们会在相同数据上产生略有不同的预测和误差，从而提高平均预测能力和鲁棒性。然而，使用整个模型集合进行预测既繁琐又计算昂贵，难以部署给大量用户，特别是当模型为大型神经网络时。为此，我们引入了一种逐层逐点的投影映射，在训练过程中将学生和教师表征映射到对齐的高维嵌入空间。所提出的方法与LoRA注入相结合，将学生模型的可训练参数减少到教师模型的1%以下，同时与其它蒸馏方法相比，显著降低了词错误率（WER），消融研究证实了这一点。与专家混合模型不同，我们的方法可以快速并行训练。

## Abstract
A simple way to improve the performance of almost any machine learning model is not to train a single but several models with diverse algorithms which will make slightly distinct kinds of predictions and errors on the same data, and thus improve the average predictions and robustness. However, making predictions using a whole ensemble of models is cumbersome and computationally too expensive to allow deployment to a large number of users, especially if the models are large neural nets. In response to this, we introduce a layer and point wise projection mapping, which maps student and teacher representations into an aligned high-dimensional embedding space during training process. The proposed approach combined with LoRA injection reduces the student model trainable parameters to less than 1% of the teacher model, while significantly improving word error rate (WER) compared to other distillation methods, as demonstrated in ablation studies. Unlike a mixture of experts, our method can be trained rapidly and in parallel.