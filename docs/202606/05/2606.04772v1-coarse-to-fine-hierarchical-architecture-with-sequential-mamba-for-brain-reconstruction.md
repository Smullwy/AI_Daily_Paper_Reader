---
title: Coarse-to-fine Hierarchical Architecture with Sequential Mamba for Brain Reconstruction
title_zh: 面向大脑重建的序列曼巴由粗到细层次架构
authors: "Hoang-Son Vo, Van-Hung Bui, Minh-Huy Mai-Duc, Tien-Dung Mai, Soo-Hyung Kim"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04772v1"
tags: ["query:fclip"]
score: 9.0
evidence: 视觉皮层图像到fMRI编码模型
tldr: 本研究针对深度视觉模型与人类视皮层层次组织对应关系这一挑战，提出CHASMBrain框架。该框架采用双流Mamba分离全局语义与局部空间信息，并通过粗到细两阶段策略实现图像到fMRI编码。在NSD数据集上，模型性能优于基线，因果消融实验揭示空间路径与语义路径对早期与高级视区的不对称因果贡献，跨被试迁移实验证实其泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04772v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the proposed hierarchical two-stage framework for image-to-fMRI reconstruction. Stage 1 (Coarse): A dual-streamMamba architecture processes global (CLS token) and local (patch tokens) visual features separately, then fuses them via an adaptive gate to predict ROI-level activations. Stage 2 (Fine): A Mamba-VAE architecture takes the visual features and ROI predictions as input, encodes them through Mamba blocks, samples from a learned latent space, and decodes to full voxellevel fMRI predictions through parallel global and local branches.\", \"page\": 4, \"index\": 3, \"width\": 690, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2606.04772v1/fig-004.webp\", \"caption\": \"Table 1. Voxel-level fMRI prediction performance. All scores are means over subjects 1, 2, 5, 7. Simple baselines and DINOv2 linear probes use the same features as our model; the probe comparison isolates the contribution of the dual-stream and coarseto-fine design. Simple baselines and probes use the same DINOv2 final-layer features as ours, but with global average pooling. SynBrain and MindSimulator follow their original feature-extraction pipelines († is reproduce results); only the dataset, subject split, and z-score normalization are matched.\", \"page\": 9, \"index\": 4, \"width\": 644, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2606.04772v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative comparison of predicted fMRI activations and downstream image reconstructions. CHASMBrain accurately preserves spatial topographic patterns and enables the downstream decoder (MindEyeV2) to reconstruct visual semantics that closely align with the original stimuli.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2606.04772v1/fig-001.webp\", \"caption\": \"Fig. 3. Spatial analysis of dual-stream correlations. Left: Early visual areas (V1/V2) are primarily driven by the Local branch (blue), while higher-order semantic areas are driven by the Global branch (red). Right: 3D visualization showing distinct functional alignments.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 212}]"
motivation: 探究现代视觉模型与人类视觉系统层次组织间的对应关系，以提升脑编码模型的生物可解释性。
method: 提出CHASMBrain，采用双流Mamba与粗到细策略，先预测ROI级激活再精细解码为全脑体素响应。
result: 在NSD上取得0.429的Pearson相关和0.261的MSE，超越基线，且因果实验显示空间流锁定于早期皮层、语义流贡献于高阶区域。
conclusion: CHASMBrain不仅预测性能领先，更因果性地揭示了视觉处理流的专门化分工，并展现出跨被试的共享视觉表征。
---

## 摘要
理解深度视觉表征与人类视觉系统之间的关系是计算神经科学中的一个基础性挑战。尽管现代视觉模型在图像识别中取得了强大性能，但它们与人类视觉皮层层次化组织的对应关系仍是一个悬而未决的问题。本研究提出了 CHASMBrain，一种新颖的图像到功能磁共振成像编码的层次化两阶段框架。受视觉皮层功能组织的启发，我们的架构利用双流曼巴设计，显式地分离并处理全局语义令牌和局部空间图像块。我们采用由粗到细的策略：第一阶段预测去噪的感兴趣区级激活，第二阶段使用曼巴变分自编码器将这些粗糙的响应细化为全脑体素级预测。在自然场景数据集上的实验表明，我们的方法达到了 0.429 的皮尔逊相关系数和 0.261 的均方误差，优于所有评估的基线方法，包括岭回归和 DINOv2 线性探针。除预测性能外，因果分支消融实验揭示了一种不对称的特化现象：图像块流特异性地锁定于早期视觉皮层（视网膜拓扑区域），而类别标记流向高阶区域提供更广泛的语义上下文——这种对应关系是因果性的，而非仅是相关性的。跨被试迁移实验进一步表明，学习到的骨干网络能够在极少的每被试适应下跨个体泛化，这表明模型捕获了一种共享的、与被试无关的视觉表征。

## Abstract
Understanding the relationship between deep visual representations and the human visual system is a fundamental challenge in computational neuroscience. While modern vision models achieve strong performance in image recognition, their correspondence with the hierarchical organization of the human visual cortex remains an open question. In this study, we propose CHASMBrain, a novel hierarchical two-stage framework for image-to-fMRI encoding. Our architecture leverages a dual-stream Mamba design to explicitly separate and process global semantic tokens and local spatial patches, motivated by the functional organization of the visual cortex. A coarse-to-fine strategy is employed: Stage 1 predicts denoised ROI-level activations, while Stage 2 refines these coarse responses into full voxel-level predictions using a Mamba-VAE. Experiments on the Natural Scenes Dataset (NSD) demonstrate that our method achieves a Pearson correlation of 0.429 and an MSE of 0.261, outperforming all evaluated baselines including ridge regression and DINOv2 linear probes. Beyond predictive performance, causal branch-ablation experiments reveal an asymmetric specialization: the patch stream is specifically locked to early visual cortex (retinotopic regions), while the CLS stream contributes broader semantic context to higher-order areas -- a correspondence that holds causally, not merely correlationally. Cross-subject transfer experiments further show that the learned backbone generalizes across individuals with minimal per-subject adaptation, suggesting the model captures a shared, subject-agnostic visual representation.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：本文高度契合读者关注的大脑编码、fMRI表征对齐与多视角约束，提供了从视觉模型到脑活动映射的新范式。
- **启发与意义**：通过因果干预验证视觉通路特化，将“相关性”分析升级为“因果性”证据，为解释性神经编码模型树立了新标准。
- **可借鉴点**：双流 Mamba 的显式分离设计、由粗到细的噪声抑制策略、以及分支消融与跨被试迁移实验方案均可直接借鉴于脑解码或表征对齐研究。
- **阅读建议**：建议重点阅读因果消融与跨被试泛化部分，其方法论对神经先验建模和多视角约束构建有直接参考价值，可复现性强。

## 1. 论文的核心问题与整体含义
- **核心问题**：理解深度神经网络所学的视觉表征与人类视觉皮层层级化功能组织之间的对应关系，特别是局部空间细节与全局语义在脑编码中的因果角色。
- **整体含义**：提出一个可解释、生物启发的图像到 fMRI 编码模型，不仅追求高预测精度，更通过可控干预揭示不同信息流对早期和高阶视区的因果影响，推动编码模型从“黑箱预测”走向“机制理解”。

## 2. 论文提出的方法论
- **整体框架**：构建名为 CHASMBrain 的层次化两阶段架构，遵循由粗到细（coarse-to-fine）原则。第一阶段产生感兴趣区（ROI）级的去噪激活，第二阶段将其细化为全脑体素级预测。
- **第一阶段：双流 Mamba 粗预测**
  - **视觉特征提取**：使用预训练 DINOv2-ViT-B/14 提取全局 `CLS` 令牌和 256 个局部 `patch` 令牌。
  - **ROI 聚合降噪**：将体素聚类为约 524 个 ROI，每个 ROI 内取 top‑$p$%（$p=70\%$）的高激活值聚合，抑制低激活噪声。
  - **全局分支**：`CLS` 令牌经投影后通过 Mamba 块建模高层语义。
  - **局部分支**：`patch` 令牌经投影、位置嵌入后通过更深层 Mamba 块，再池化并通过 MLP 输出 ROI 响应。
  - **自适应融合门**：通过可学习的门控网络产生融合权重 $\alpha$，得到最终 ROI 预测 $\hat{\mathbf{r}} = \alpha \cdot \mathbf{r}_g + (1-\alpha) \cdot \mathbf{r}_l$。
  - **对比对齐**：加入 InfoNCE 损失，将视觉与脑响应的投影拉近，增强跨模态判别性。
  - **损失函数**：$L_{\text{stage1}} = L_{\text{MSE}} + \gamma L_{\text{Pearson}} + \lambda L_{\text{InfoNCE}}$。
- **第二阶段：Mamba-VAE 精细预测**
  - **输入融合**：将 DINOv2 特征与第一阶段粗预测 $\hat{\mathbf{r}}$ 拼接成统一序列。
  - **Mamba 编码器**：建模视觉与预调神经结构间的交互。
  - **变分潜空间**：通过 VAE 头采样潜变量 $z$，捕获试验间变异，训练时采样，推理时用均值 $\mu$。
  - **特征分流与双解码**：编码后的序列按令牌角色分为整体解码和 ROI 残差解码两路。
    - 整体解码器：用可学习查询令牌通过 Mamba 产生全脑体素预测。
    - ROI 残差解码器：每 ROI 的令牌通过 MLP 学一个残差 $\Delta_k$，粗预测加残差得精细响应 $v_k = r_k + \Delta_k$。
  - **融合与幅度恢复**：学习全局融合权重 $\alpha$，并通过基于视觉特征的缩放层恢复最终幅值。
  - **损失函数**：$L_{\text{stage2}} = L_{\text{MSE}} + \beta L_{\text{KL}}$。

## 3. 实验设计
- **数据集与场景**：自然场景数据集（NSD），使用被试 1、2、5、7，每人约 9,000 对训练、1,000 对测试，覆盖视觉皮层约 15,000 个体素。
- **基准与方法对比**：
  - 简单基线：岭回归（最优 $\alpha$）、MLP、GRU、LSTM（均使用相同 DINOv2 平均池化特征）。
  - 线性探针：DINOv2 第 3、6、9、12 层的线性回归。
  - 先前工作：SynBrain（NeurIPS 2025）、MindSimulator（ICLR 2025）。
- **评价指标**：体素级皮尔逊相关系数（↑）和均方误差（MSE，↓），并以噪声天花板估计理论上限（约 $r \approx 0.60$）。

## 4. 资源与算力
- 使用 **单张 NVIDIA RTX 4090 GPU**，基于 PyTorch 实现。
- 第一阶段训练 150 个 epoch，批大小为 32；第二阶段训练 150 个 epoch，批大小为 16。
- 文中未提供具体的训练时长（小时）或总能耗。

## 5. 实验数量与充分性
- **主要对比实验**：1 组完整体素级性能对比表，涵盖 9 种方法（含各层探针），具有多角度比较。
- **消融实验**：深度消融（Stage 1 架构替换为 Transformer/单流 Mamba；Stage 2 细化策略消融，包括 MLP、扩散、VAE），聚类粒度（10/30/100 体素和 Schaefer 图谱），top‑p 阈值敏感性。
- **因果分析**：4 种推理时间分支消融（全模型、仅局部流、仅全局流、交换路由），按 early/mid‑ventral/higher 皮层分组统计。
- **泛化实验**：多源被试到目标被试的迁移实验（冻结主干，微调轻量头），含不同数量的源被试。
- **定性分析**：预测 fMRI 的皮层表面映射与下游图像重建。
- 实验数量与维度充分，消融细致，对比公平（均使用同一特征源并匹配归一化），且包含跨被试与因果干预的设计，客观性较强。

## 6. 论文的主要结论与发现
- CHASMBrain 取得 Pearson $0.429$、MSE $0.261$，优于所有对比基线，且相比最强 DINOv2 探针 MSE 降低 26.9%。
- 因果分支消融证明：局部图像块流（空间细节）特异性锁定于早期视觉皮层，全局语义流对高阶区贡献更广；交换路由导致早期区性能下降约 45%（不对称），确认为因果性特化。
- 跨被试迁移证实模型主干学到的是与被试无关的共享视觉表征，轻量头部即可适应个体体素布局。
- 两阶段去噪——ROI 级语义对齐再体素级校准——有效应对 fMRI 高噪声。

## 7. 优点
- **生物启发的双流设计**：显式解耦“what”和“where”信息，且通过 Mamba 的选择性状态空间机制有效地关注任务相关信号。
- **由粗到细的降噪机制**：ROI 级聚合和 top‑p 选择策略，使第一阶段能屏蔽低激活噪声，第二阶段聚焦精细恢复。
- **因果验证**：分支消融与路由交换实验，将模型组分与特定皮层区建立起因果联系，提升了解释性。
- **跨被试泛化能力**：证明主干学到的是人脑共性表征，为非共享个体数据的场景提供了可能。

## 8. 不足与局限
- **方法层面**：依赖静态、面向对象图像的 DINOv2 编码器，可能缺乏对自然视觉的时间动态建模能力；fMRI 信号噪声天花板限制了预测性能的绝对上限。
- **实验覆盖**：仅在 NSD 数据集上评估，缺乏跨数据集（如 BOLD5000、Algonauts）的验证；下游图像重建受解码器自身幻觉偏差影响，不能直接等同于信号恢复质量。
- **应用限制**：当前模型仅限于静物图像编码，扩展到视频或多模态刺激时需引入时序模块；ROI 聚类高度适应 NSD 特定体素空间，迁移到不同采集协议或脑区定义时可能需要重新适配。

## 9. （无额外标题要求，但已按要点完成）
（完）
