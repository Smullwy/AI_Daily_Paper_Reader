---
title: "DynFS-MoE: Dynamic Functional-Structural Mixture-of-Experts for Post-Traumatic Epilepsy Diagnosis"
title_zh: DynFS-MoE：用于创伤后癫痫诊断的动态功能-结构混合专家模型
authors: "Jun-En Ding, Spencer Chen, Henry Noren, Daniel Valdivia, Christine Yohn, Suhina Patel, Taylor Zink, Hai Sun, Feng Liu"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16203v2"
tags: ["query:fclip"]
score: 9.0
evidence: 使用功能性和结构性MRI进行癫痫诊断
tldr: 创伤后癫痫（PTE）的早期识别因复杂脑结构与功能改变而困难，本文提出动态多模态混合专家框架DynFS-MoE，整合功能与结构MRI，通过时间感知编码和类别条件路由动态调度专家，在三个二分类任务中超越静态融合基线，且可解释性分析揭示了有意义的脑区交互，为PTE诊断与风险分层提供了新方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16203v2/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed DynFS-MoE framework.\", \"page\": 3, \"index\": 1, \"width\": 725, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2606.16203v2/fig-002.webp\", \"caption\": \"Table 1. Performance comparison across different classification tasks. Statistical significance was evaluated between DynFS-MoE and the best remaining baseline using Welch’s t-test (* p < 0.05, ** p < 0.01, *** p < 0.001).\", \"page\": 7, \"index\": 2, \"width\": 660, \"height\": 785}]"
motivation: PTE早期诊断困难，需捕捉脑结构与功能的复杂交互改变。
method: 设计DynFS-MoE框架，结合时间感知功能-结构编码与类条件专家路由，动态分配模态特定及跨模态专家权重。
result: 在三个二分类任务上一致优于静态融合方法，并解析出具有诊断意义的ROI交互模式。
conclusion: 该动态多模态混合专家框架能有效提取类依赖的脑交互特征，为PTE提供可解释的诊断手段。
---

## 摘要
创伤后癫痫（PTE）是创伤性脑损伤（TBI）的一种严重并发症，但由于其引发的大脑复杂结构和功能改变，早期识别仍然具有挑战性。为此，我们提出了一种动态多模态混合专家（MoE）框架，通过时间感知的功能-结构编码和类别条件化的专家路由，整合功能性和结构性磁共振成像。在该框架中，模态特定和跨模态专家学习互补表示，而模态-类别MoE（MCoE）模块根据每个分类目标动态分配专家权重。在三个二分类任务上的实验结果表明，该框架一致优于静态融合基线，高可解释性分析进一步揭示了有意义的感兴趣区域（ROI）交互作用。这种动态多模态专家框架有效捕捉了类别依赖的脑交互模式，并为PTE诊断和风险分层提供了一种可解释的方法。

## Abstract
Post-traumatic epilepsy (PTE) is a severe complication of traumatic brain injury (TBI), yet early identification remains challenging due to the complex structural and functional alterations it induces in the brain. To address this, we propose a dynamic multimodal Mixture-of-Experts (MoE) framework that integrates functional and structural MRI through time-aware functional-structural encoding and class-conditioned expert routing. Within this framework, modality-specific and cross-modal experts learn complementary representations, while a Modality-Class MoE (MCoE) module dynamically dispatches expert weights according to each classification objective. Experimental results across three binary classification tasks demonstrate that the framework consistently outperforms static fusion baselines, and high-interpretability analyses further reveal meaningful region-of-interest (ROI) interactions. This dynamic multimodal expert framework effectively captures class-dependent brain interaction patterns and provides an interpretable approach for PTE diagnosis and risk stratification.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：本文与“fMRI表示”和“多模态融合”中度相关，与“神经先验”和“脑解码”弱相关。它主要利用fMRI动态连接性和结构MRI进行多模态分类，而非直接进行脑编码或解码。
- **启发与意义**：其类别条件化的动态专家路由为多模态医学影像分析提供了一种自适应模态加权思路，对多视图约束和表示对齐有借鉴价值，但缺乏神经先验的直接建模。
- **可借鉴点**：时间感知的功能-结构编码模块（将fMRI划分为时间片段并融入结构连接）和基于互信息的可解释性正则项，可用于其他脑影像表示学习任务。
- **阅读建议**：若关注动态功能连接与多模态融合策略，可精读第2-3节；若注重可解释性分析，可略读实验部分及脑区交互可视化。整体对纯粹的脑解码研究启发有限。

## 1. 论文的核心问题与整体含义
- **研究背景**：创伤后癫痫（PTE）是创伤性脑损伤（TBI）的严重长期并发症，早期诊断困难，仅靠结构性MRI或功能MRI往往预测精度不足，且难以捕捉结构和功能异常的动态相互作用。
- **核心问题**：如何有效融合动态功能MRI和结构MRI信息，自适应地利用不同模态在不同诊断类别中的贡献，提升PTE与TBI、健康对照的分类准确性和可解释性。
- **整体含义**：提出一种动态多模态混合专家框架，通过类别条件化路由实现模态权重自适应分配，克服静态融合的弊端，并为PTE诊断和风险分层提供可解释的脑区交互模式。

## 2. 方法论
### 核心思想
- 将fMRI时间序列切分为若干时间片段（patches），并将每个片段与结构连接嵌入融合，形成统一的模态patch表示。
- 使用模态特定混合专家（MSoE）提取各自模态的专用表示，同时引入跨模态专家捕捉交互。
- 设计模态-类别MoE（MCoE），根据诊断类别动态分配专家权重，使不同类别侧重不同模态和专家。
- 引入基于条件互信息的正则项，鼓励不同类别选择不同的专家-模态组合，增强可解释性。

### 关键技术细节
- **时间感知功能-结构编码器**：
  - fMRI BOLD信号 $F \in \mathbb{R}^{R \times T}$ 分割为 $K$ 个长度 $L$ 的片段 $F^{(k)}$。
  - 结构连接矩阵 $S \in \mathbb{R}^{R \times R}$ 取上三角元素压缩为向量 $s$。
  - 两者经MLP投影为 $D$ 维嵌入，得到 patch 集 $\{f^{(1)},..., f^{(K)}, s\}$。
  - 对每个fMRI patch，使用缩放点积注意力融合结构信息：$\tilde{f}^{(k)} = f^{(k)} + \alpha^{(k)} W_V s$，其中 $\alpha^{(k)}$ 为注意力系数，再经MLP得到跨模态融合嵌入。
- **模态特定混合专家（MSoE）**：对任意 patch $p_i$，通过门控 $\pi_j(p_i) = \text{softmax}_j(W_g p_i)$ 输出到 $J$ 个专家的概率，加权合并专家输出 $z_{i}^{\text{spec}} = \sum_j \pi_j(p_i) \phi_j(p_i)$。
- **模态-类别混合专家（MCoE）**：
  - 输入为所有精炼 patch 嵌入 $Z = \{z_i\}$。
  - 计算类别条件路由logits $L = f(Z) \odot \Phi$，$\Phi \in \mathbb{R}^{|P| \times J \times C}$。
  - 派发权重 $D_{ijy} = \frac{e^{L_{ijy}}}{\sum_{i'} e^{L_{i'jy}}}$ 表示给定专家和类别时 patch 的概率；组合权重 $C_{jy} = \frac{e^{\bar L_{jy}}}{\sum_{j'} e^{\bar L_{j'y}}}$。
  - 专家输入为加权 patch 嵌入加类别嵌入 $c_y$，输出再按组合权重聚合得到类别特定特征 $h_y$。
- **可解释性正则项**：最大化条件互信息 $I(P;E|Y)$，使每个类别下 patch 与专家选择具有高相关性，损失函数为 $\min_\theta \mathbb{E}[L_{cls} - \alpha I_\theta(P;E|Y)]$。

## 3. 实验设计
- **数据集**：自采数据，健康对照 (HC) 26例，TBI 24例，PTE 19例。使用3T Siemens Prisma扫描，包含扩散加权成像。通过SPM、MRtrix3、FSL预处理，使用AAL 116脑区构建结构连接和功能连接矩阵。
- **任务**：三个二分类任务：HC vs. TBI、HC vs. PTE、TBI vs. PTE。
- **基准方法**：
  - 单模态：fMRI-FC+MLP、sMRI+MLP。
  - 静态融合：EarlyFusion。
  - 图方法：Fusion+GCN, FBNetGen, IBGNN, IBrainGNN, AGIIBM。
  - Transformer类：MultiViT, Contrasformer。
- **评估指标**：准确率(ACC)、AUC、F1分数，并采用Welch t检验评估显著性。

## 4. 资源与算力
- 论文未明确提及所使用的GPU型号、数量、训练时长或任何算力相关细节。推测为常规计算环境，但未给出具体信息。

## 5. 实验数量与充分性
- **实验组**：1个主实验结果表（3个任务 × 12种方法），2组消融实验（类别条件路由 vs 均匀先验），以及1组可解释性可视化分析。
- **充分性**：对比了多种类型基线（MLP、GNN、Transformer），进行了消融验证关键模块（类别条件路由），并从统计显著性、可解释性角度进行分析，实验设计较为完整。
- **客观公平性**：所有方法在同一数据集上评估，采用多次重复汇报标准差，统计检验合理。但因数据量较小（共69例），结果外推性可能受限。

## 6. 主要结论与发现
- DynFS-MoE在三个二分类任务上一致超越所有静态融合和单模态基线，尤其在PTE相关任务中显著提升（HC vs PTE AUC 0.84, TBI vs PTE AUC 0.82）。
- 类别条件化路由比均匀先验显著提升PTE分类性能，表明自适应模态权重分配至关重要。
- 可解释性分析显示，PTE偏好早期fMRI片段而降低结构权重，TBI上调中期fMRI和结构特征，且PTE展现出特异的前额-枕叶和听觉皮层动态招募模式，与已知病理机制相符。

## 7. 优点
- **动态融合策略**：将fMRI时间分段并与结构信息交互，保留了时序动态性，比传统静态功能连接更具表达能力。
- **类别条件专家路由**：使不同诊断类别可自适应选择模态和专家，避免单一模态主导，克服了模态竞争和信息损失。
- **可解释性设计**：互信息正则与可视化分析不仅提升性能，还提供了神经生物学上有意义的脑区交互模式，增强了临床信任度。
- **统一的MoE框架**：将模态特定、跨模态以及类别级路由巧妙结合，模型结构新颖且合理。

## 8. 不足与局限
- **小样本问题**：总样本量仅69例，且PTE仅19例，可能存在过拟合风险，跨站点推广性未验证。
- **缺乏外部验证**：未在公开数据集或其他队列上测试，结果泛化性存疑。
- **计算资源信息缺失**：未说明训练成本，难以评估实际应用可行性。
- **模态单一局限**：仅融合rs-fMRI和结构连接，未纳入其他常用模态（如EEG、DTI指标），可能遗漏有用信息。
- **可解释性局限**：可视化基于学习到的路由权重，仅间接反映脑区重要性，未与临床表型进行严格的统计关联分析。

## 9. 附加要点
- **方法细节**：模型推断时，MCoE模块的类别条件路由需要预知类别标签，论文未说明测试时如何处理；可能采用了类似输入依赖的路由（类别嵌入或许由输入特征生成），但具体实现未详述。
- **正则的权衡**：互信息正则系数 $\alpha$ 的设置对结果影响未做敏感度分析。
- **可应用性**：作为一种辅助诊断工具，其临床部署需考虑模型校准和决策阈值，论文未涉及。

（完）
