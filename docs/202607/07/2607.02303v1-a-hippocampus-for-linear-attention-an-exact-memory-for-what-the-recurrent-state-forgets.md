---
title: "A Hippocampus for Linear Attention: An Exact Memory for What the Recurrent State Forgets"
title_zh: 用于线性注意力的海马体：为循环状态遗忘的内容提供精确记忆
authors: Wanyun Cui
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02303v1"
tags: ["query:fclip"]
score: 7.0
evidence: 海马体启发的线性注意力记忆结构
tldr: 线性注意力模型将上下文压缩为固定大小的循环状态，但会遗忘早期信息。本文受互补学习系统启发，提出HOLA方法，在压缩记忆基础上添加有界精确KV缓存，形成半参数测试时记忆。缓存基于预测残差写入，解耦RMSNorm读取实现尖锐检索。在340M参数训练后，HOLA显著降低语言模型困惑度，优于全注意力Transformer，并在长上下文检索任务中保持鲁棒。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02303v1/fig-002.webp\", \"caption\": \"Figure 2: HOLA: semiparametric test-time memory. Every token updates the recurrent state memory (≈ neocortex; lossy, O(1)), while the tokens with large delta-rule write magnitude β ·∥e∥ are additionally kept as exact KV pairs in a bounded exact-KV memory (≈ hippocampus). The read-out follows the semiparametric form ot = q⊤t St + λtgt(qt): a compressive state estimate plus a non-parametric exact-KV read, instantiated with a sharpened softmax in Sec. 3.4.\", \"page\": 4, \"index\": 2, \"width\": 739, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2607.02303v1/fig-001.webp\", \"caption\": \"Table 1: 340M / SlimPajama-15B / ctx-2048 main comparison (↓/↑ lower/higher better). Commonsense is broken out per task (ARCe, PIQA, Wino, LMBa = accuracy; ARCc, Hella = acc_norm), and Avg. is their six-task mean. Borrowed rows are from recipe-matched papers (Transformer++/GLA/GSA (Du et al., 2025); DeltaNet/GDN (Tumma et al., 2026); KDA is Kimi Delta Attention (Kimi Team et al., 2025), with numbers borrowed from Tumma et al., 2026); their pertask values reproduce each source’s reported commonsense average exactly. Bold = best per column among the sub-quadratic models (Transformer++ excluded, as it is a different, quadratic-cost class). “HOLA+recency” is our own matched position-eviction variant (HOLA’s cache but with recency instead of β·∥e∥ eviction), with retrieval via the corrected full-recompute evaluation path. Commonsense differences among our three models (GDN / HOLA+recency / HOLA) are within single-seed noise (<0.7 on the six-task average); the cache’s gains are in perplexity and retrieval.\", \"page\": 7, \"index\": 1, \"width\": 830, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2607.02303v1/fig-003.webp\", \"caption\": \"Table 3: RULER accuracy (340M), compact 2k–8k multi-task snapshot. Transformer++ is the full-attention ceiling (RoPE, max position 8192); we compare three sub-quadratic models (GDN / HOLA+recency / HOLA). Single-needle = S-NIAH-1/2/3 (columns 1–3); multi-needle = multikey-1 (MK1), multi-value (MV), multi-query (MQ). Bold = best sub-quadratic model per cell. Transformer++ is strongest within its 2k training length but collapses to 0 under RoPE extrapolation at 4k+, where HOLA degrades gracefully. The 16k/32k S-NIAH-1 trend is shown in Fig. 1b.\", \"page\": 9, \"index\": 3, \"width\": 518, \"height\": 349}]"
motivation: 线性注意力的压缩状态会导致早期关键信息被覆盖和遗忘，需要补充机制保留重要关联。
method: 在原有delta-rule压缩状态上，添加基于预测残差写入、解耦RMSNorm读取的精确KV缓存，构成互补记忆系统。
result: "HOLA将Wikitext困惑度降至22.92（降低16.1%），低于全注意力Transformer的26.88，并在32k tokens的检索任务中展现出最佳鲁棒性。"
conclusion: 结合压缩与精确记忆的HOLA有效解决了线性注意力的遗忘问题，大幅提升了长文本理解能力。
---

## 摘要
线性注意力和状态空间语言模型将前缀压缩为固定大小的循环状态，以有损的精确记忆为代价实现了O(1)内存：当许多键-值对竞争时，更早的事实会被覆盖，针状回忆能力下降。受互补学习系统的启发，我们为线性注意力提供了一个海马体补充。HOLA（海马体线性注意力）保留了常见的delta规则状态作为压缩记忆，并添加了一个有界的精确KV缓存，形成了一个半参数化的测试时记忆：状态建模可线性压缩的结构，而缓存存储不应强制通过该状态的关联。缓存在没有学习驱逐模块的情况下写入，保留具有大beta * ||e||（实际提交给状态的预测残差）的token；然后解耦的RMSNorm-gamma缓存读取将这些精确的KV对转化为锐利的检索，而不是软平均。在15B SlimPajama token上训练的3.4亿参数模型上，HOLA将Wikitext困惑度从27.32降低到22.92（-16.1%），低于全注意力Transformer++的26.88，并将LAMBADA困惑度从30.95降低到30.26。它还实现了最佳的线性上下文检索，并在长达32k token的RULER针在草堆回忆测试上比GDN或匹配的HOLA+最近性缓存保持更强的鲁棒性（训练长度的16倍）。

## Abstract
Linear-attention and state-space language models compress the prefix into a fixed-size recurrent state, yielding O(1) memory at the cost of a lossy exact memory: when many key--value associations compete, earlier facts are overwritten and needle recall degrades. Inspired by Complementary Learning Systems, we give linear attention a hippocampal complement. HOLA (Hippocampal Linear Attention) keeps the usual delta-rule state as a compressive memory and adds a bounded exact KV cache, forming a semiparametric test-time memory: the state models linearly compressible structure, while the cache stores associations that should not be forced through that state. The cache writes without a learned eviction module, keeping tokens with large beta * ||e||, the prediction residual actually committed to the state; a decoupled RMSNorm-gamma cache read then turns these exact KV pairs into sharp retrieval rather than soft averaging. At 340M parameters trained on 15B SlimPajama tokens, HOLA lowers Wikitext perplexity from 27.32 to 22.92 (-16.1%), below a full-attention Transformer++ (26.88), and improves LAMBADA perplexity from 30.95 to 30.26. It also achieves the best linear in-context retrieval and remains much more robust than GDN or a matched HOLA+recency cache on RULER needle-in-a-haystack recall out to 32k tokens (16x its training length).