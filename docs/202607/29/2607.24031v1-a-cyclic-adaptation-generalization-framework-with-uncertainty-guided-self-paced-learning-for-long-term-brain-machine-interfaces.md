---
title: A Cyclic Adaptation-Generalization Framework with Uncertainty-Guided Self-Paced Learning for Long-Term Brain-Machine Interfaces
title_zh: 面向长期脑机接口的基于不确定性引导自步学习的循环自适应-泛化框架
authors: "Jiyu Wei, Di Hong, Zhanjie Zhang, Dazhong Rong, Qinming He, Yueming Wang"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24031v1"
tags: ["query:fclip"]
score: 8.0
evidence: 不确定性引导的自定进度学习用于长期脑解码
tldr: 针对脑机接口长期部署中因神经漂移导致解码性能下降的挑战，现有域适应或域泛化方法忽视细粒度子域分布漂移 的问题，本文提出UnSPC框架。该框架通过不确定性引导的自步伪标记机制迭代筛选可靠样本，并创新地循环集成域适应与域泛化，逐步缓解全局与子域漂移，在多个数据集上验证了有效性，为长期稳定BMI控制奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24031v1/fig-005.webp\", \"caption\": \"Fig. 1. UnSPC consists of two key modules: (i) UnSPL mines reliable pseudo-labeled samples; (ii) CycAG cyclically alternates DA and DG.\", \"page\": 1, \"index\": 5, \"width\": 508, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-004.webp\", \"caption\": \"Fig. 2. The overall framework of our UnSPC. A: Robust Pre-training Decoder (RPD) pre-trained on the source domain for initialization. B: Uncertaintyguided Self-paced Pseudo-labeling (UnSPL) selecting and weighing reliable samples on the target domain to form a reliable domain. C: Cyclic AdaptationGeneralization (CycAG), cyclically alternating between adaptation and generalization using reliable pseudo-labeled samples.\", \"page\": 3, \"index\": 4, \"width\": 1040, \"height\": 647}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-002.webp\", \"caption\": \"Fig. 3. Feature distributions and motor trajectories of ideal (supervised), static (uncalibrated), alignment-based method (WDGRL) and our UnSPC on C0–C1. A and B: Feature distributions under different alignments colored by values of speed and direction. C: Motor trajectories colored by trial direction.\", \"page\": 6, \"index\": 2, \"width\": 1035, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-001.webp\", \"caption\": \"TABLE I RECALIBRATION PERFORMANCE COMPARISON (BOLD FOR THE BEST AND UNDERLINE FOR THE SECOND-BEST)\", \"page\": 6, \"index\": 1, \"width\": 1044, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-003.webp\", \"caption\": \"TABLE II ABLATION RESULTS OF UNSPC\", \"page\": 7, \"index\": 3, \"width\": 508, \"height\": 248}]"
motivation: 长期脑机接口面临神经漂移，现有方法无法有效捕获细粒度子域分布漂移。
method: 提出UnSPC，结合不确定性引导自步伪标记与循环适应-泛化策略逐步对齐目标分布。
result: 在多个神经解码数据集上，UnSPC展现出优于现有方法的解码性能与鲁棒性。
conclusion: UnSPC首次循环融合域适应、域泛化与伪标记，为稳定长期BMI控制提供新思路。
---

## 摘要
脑机接口连接大脑与外部设备，在康复、人类性能增强和以人为中心的机器人技术中具有巨大潜力。然而，侵入式脑机接口因神经漂移在长期部署中面临关键挑战，神经漂移会随时间推移降低解码性能，需要频繁重新校准。现有缓解神经漂移的方法通常仅依赖域适应或域泛化，往往无法捕获神经子域间的细粒度分布偏移，导致性能有限。为克服这些限制，我们提出了不确定性引导的自步循环框架（UnSPC），该框架在不确定性引导的自步伪标签机制（UnSPL）下协同域适应与域泛化进行目标域精炼。为处理跨域的子域神经漂移，提出UNSPL通过噪声鲁棒排序策略迭代挖掘可靠的伪标签样本进行进一步微调。利用这些高质量样本，我们引入新颖的循环自适应与泛化策略，在迭代循环中整合域适应与域泛化，逐步缓解全局与子域漂移。该循环过程能有效对齐不断演变的目标分布，同时保持鲁棒且可迁移的表示，从而减轻长期神经漂移下的性能退化。在多个神经解码数据集上的大量实验证明了UnSPC的有效性和鲁棒性。据我们所知，我们提出的UnSPC是首个以循环方式将域适应、域泛化与伪标签相结合的工作，为实现稳定的长期脑机接口控制铺平了道路。

## Abstract
Brain-Machine Interfaces (BMIs), which link the brain to external devices, hold great potential in rehabilitation, human performance augmentation, and human-centered robotics. However, invasive BMIs face a critical challenge for long-term deployment due to neural drift, which degrades decoding performance over time and necessitates frequent recalibration. Existing methods designed to mitigate neural drift typically rely on either domain adaptation (DA) or domain generalization (DG) alone and often fail to capture fine-grained distribution shifts across neural subdomains, resulting in limited performance. To overcome these limitations, we propose Uncertainty-guided Self-paced Cycling (UnSPC), a robust framework that synergizes DA and DG for target domain refining under an Uncertainty-guided Self-paced Pseudo-labeling (UnSPL) mechanism. To handle subdomain neural drift across domains, UNSPL is proposed to iteratively mine reliable pseudo-labeled samples with a noise-robust ranking strategy for further fine-tuning. Leveraging these high-quality samples, we introduce a novel Cycling Adaptation and Generalization (CycAG) strategy, which integrates DA and DG within an iterative cycle to progressively mitigate both global and subdomain drift. This cyclic process enables effective alignment to evolving target distributions while preserving robust and transferable representations, thereby mitigating performance degradation under long-term neural drifts. Extensive experiments on multiple neural decoding datasets demonstrate the effectiveness and robustness of UnSPC. To our knowledge, our proposed UnSPC is the first to cyclically integrate DA and DG with pseudo-labeling, paving the way toward stable long-term BMI controls.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议

* **关联方向**：弱相关。读者聚焦 fMRI 表征与脑编码/解码，而本文针对侵入式脑机接口的运动神经解码，模态不同，但都面临跨域/跨时间分布漂移的挑战。
* **启发与意义**：文中将不确定性建模、自步学习与循环域对齐结合的策略，可为 fMRI 跨个体、跨扫描段落的稳健表征对齐提供方法论思路。
* **可借鉴点**：不确定性引导的伪标签筛选与加权（UnSPL）可用于 fMRI 回归任务中生成可靠伪标签；循环交替地执行域自适应与域泛化的设计（CycAG）可迁移至多源域或多扫描条件下的稳健表示学习。
* **阅读建议**：重点阅读 UnSPL 的采样与加权机制（式 9–14）以及 CycAG 的迭代交替优化策略，思考如何将其不确定性估计改为适合体素级回归的形式。

## 论文的核心问题与整体含义

* **长期脑机接口的瓶颈**：侵入式脑机接口在长期使用中因电极漂移、神经元更替、神经可塑性等导致神经信号分布偏移（neural drift），使基于源域（day 0）训练的固定解码器在后续会话（day k）性能显著下降，需要频繁重新校准。
* **现有方法局限**：域适应（DA）与域泛化（DG）分别从对齐目标域分布和学习不变表征出发，但通常孤立使用，无法兼顾适应性与泛化性；而且主要关注全局分布对齐，忽略子域（细粒度运动状态）漂移。
* **本文目标**：提出UnSPC框架，首次以循环方式整合DA与DG，并引入不确定性引导的自步伪标签机制，在无目标域标签下逐步缓解全局与子域漂移，实现长期稳定解码。

## 论文提出的方法论

* **整体框架**：UnSPC包含源域预训练（RPD）和目标域迭代精炼（UnSPL + CycAG）。
* **鲁棒预训练解码器（RPD）**：在源域通过混合解码（干净与随机掩蔽通道输入）与一致性约束（特征-标签距离对齐、师生一致性）训练主干网络，增强对电极噪声和通道缺失的鲁棒性。
* **不确定性引导的自步伪标签（UnSPL）**：
  * 对目标域每个样本估计预测不确定性：MC Dropout方差 + 模型间预测不一致（掩蔽输入与原始输入之间的差异）。
  * 自步选择：根据当前迭代轮次确定阈值，逐步放宽，按不确定性从小到大选择高置信样本作为可靠集。
  * 权重分配：按样本在可靠集内的排序指数衰减加权，使更可靠的伪标签贡献更大。
* **循环自适应-泛化（CycAG）**：
  * 域适应步骤：利用选出的可靠伪标签进行加权监督学习，同时最小化源-目标特征分布间的 MMD 距离。
  * 域泛化步骤：在目标域施加师生一致性（掩蔽版本与原始版本预测一致），并对可靠集计算加权掩蔽解码损失。
  * 循环交替：将每轮的可靠样本用于 DA 和 DG 的交替更新，使模型既对齐目标分布，又保持对未见变化的鲁棒性。
* **教师-学生范式**：教师模型参数由学生参数指数移动平均更新，用于伪标签生成和最终推理。

## 实验设计

* **数据集**：
  * 三个非人灵长类数据集（Chewie、Mihili、Jango），包含中心-外伸、随机目标、等长腕部运动任务，96通道 Utah 阵列记录 M1 区信号，跨越多天（约1天到约3个月间隔）。
  * 人类假肢控制数据集 FALCON 的一部分（作为鲁棒性验证）。
* **任务设定**：首个会话为有标签源域，后续会话为无标签目标域。
* **对比方法**：
  * DA：DA-DCF、UAN、WDGRL
  * DG：LFDA
  * 半监督：SeSA
  * 静态基线：无适应的源域解码器。
* **评估指标**：Pearson 相关系数（CC）、决定系数（R²）、均方误差（MSE）。

## 资源与算力

* 文中未提及 GPU 型号、数量或训练时长，仅说明使用 Adam 优化器，学习率 $5 \times 10^{-3}$，batch size 128 等具体超参数，但无任何硬件资源描述，**资源与算力信息缺失**。

## 实验数量与充分性

* **跨会话对比实验**：3 个 NHP 数据集，每个数据集 3 个目标会话（共 9 组跨天任务），与 6 种方法对比，报告 CC 和 R²。
* **消融实验**：在 Chewie C0→C1 下，逐步去除 RPD、RSS、RSW、整个 UnSPL、CycDA、CycDG 及循环结构，共 7 种变体。
* **超参数敏感性分析**：对 α、β、η、ϕ 分别测试 3–4 个值，观察 R² 变化。
* **人类数据鲁棒性测试**：在 FALCON 数据集 3 个目标天对比 UnSPC 与静态基线及两种 DA 方法。
* **定性分析**：展示特征分布与运动轨迹可视化。
* **评价**：实验设计较为充分，覆盖多种时间跨度、基线种类和消融维度；对比方法包含半监督、DA、DG 的代表性工作，但未与最新的半监督或无监督回归方法进行更前沿的比较；人类测试规模较小，但提供了初步的跨模态验证。

## 论文的主要结论与发现

* UnSPC 在所有对比中大幅超越静态解码器，并在完全无监督设定下普遍优于现有 DA、DG 方法及半监督方法 SeSA。
* 消融实验表明，RPD 是强健的初始化，UnSPL 的样本选择和加权步骤至关重要，去除会导致严重的噪声传播；CycAG 的循环交替设计比单独使用 DA 或 DG 更有效。
* 超参数敏感性分析显示，在较宽范围内性能稳定，方法具有较好的鲁棒性。
* 在人类假肢控制数据上也取得了最高性能，证明方法对真实 BMI 场景有一定的泛化能力。

## 优点

* 创新性地协同 DA 和 DG，通过循环迭代使两者互补，而非割裂看待。
* 提出的 UnSPL 用不确定性引导自步采样与排序加权，有效缓解回归任务中伪标签噪声问题，设计合理且计算清晰。
* 在多个长期漂移场景和消融中系统验证了各组件的必要性。
* 方法整体为无监督，无需目标域任何真实标签，实用性强。

## 不足与局限

* **模态单一**：仅在侵入式微电极阵列的神经信号上验证，未扩展到 EEG/fMRI 等其他神经模态，代表对齐方法可能不一定直接适用。
* **任务与个体泛化性有限**：仅包含 3 只猴子和少量人类数据，运动任务较为简单，尚未在更复杂的多关节精细控制及大量个体上检验。
* **伪标签阈值策略依赖**：自步进度的增长率 ∆γ 和初始比例 γ0 需根据数据集调节，文中未系统探索其自适应设置。
* **未考虑在线部署**：方法依赖离线迭代，实时性未讨论，实际系统部署的可行性待验证。
* **基线比较不够前沿**：对比的 DA/DG 方法多为 2019–2023 年的典型方法，未包括最新的半监督回归或自监督迁移学习工作。
* **算力与时间成本未报告**：未说明训练时长、推理延迟，不利于评估资源需求。

## 参考资源与链接

* 论文链接：https://arxiv.org/pdf/2607.24031v1

（完）
