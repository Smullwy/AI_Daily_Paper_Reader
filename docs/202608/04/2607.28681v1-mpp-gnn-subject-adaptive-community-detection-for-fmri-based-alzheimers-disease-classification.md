---
title: "MPP-GNN: Subject-Adaptive Community Detection for fMRI-Based Alzheimer's Disease Classification"
title_zh: MPP-GNN：面向基于fMRI的阿尔茨海默病分类的个体自适应社区检测
authors: "Yang Zhang, Xiao Zhou, Jonathan Warrell, Avram Holmes, Xuan Zhang, Mark Gerstein"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.28681v1"
tags: ["query:fclip"]
score: 9.0
evidence: 跨被试的自适应fMRI脑表征
tldr: 基于功能磁共振成像的脑网络分析中，现有图神经网络方法假设所有受试者具有预设的功能模块，忽略个体差异。本文提出元概率池化图神经网络（MPP-GNN），通过耦合双层优化自适应发现个体特异性模块，并将其作为先验指导图边细化和表示学习，用于阿尔茨海默病分类。在两个公开数据集上取得最优性能，且发现的模块与Yeo脑图谱高度一致，揭示了阿尔茨海默病的网络级去分化模式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.28681v1/fig-004.webp\", \"caption\": \"Fig. 1. The architecture of MPP-GNN, where the top row illustrates the data preprocessing, the middle row depicts the outer-level optimization and the bottom row details the inner-level AP-HPM and PERM modules.\", \"page\": 4, \"index\": 4, \"width\": 1052, \"height\": 790}, {\"url\": \"assets/figures/arxiv/2607.28681v1/fig-001.webp\", \"caption\": \"TABLE II CLASSIFICATION RESULTS OVER FIVE-FOLD CROSS-VALIDATION (MEAN ± STANDARD DEVIATION, %). THE BEST AND SECOND-BEST RESULTS ARE SHOWN IN BOLD AND UNDERLINED, RESPECTIVELY.\", \"page\": 7, \"index\": 1, \"width\": 829, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2607.28681v1/fig-002.webp\", \"caption\": \"Fig. 2. The chord diagram (left) visualizes the network pair that has consistently high system-level connections in most evaluated models. The glass brain (right) depicts the connections in another view with topological relations.\", \"page\": 8, \"index\": 2, \"width\": 860, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2607.28681v1/fig-003.webp\", \"caption\": \"Fig. 3. AP-HPM interpretability. (a) The consensus partition is obtained from the cross-subject co-assignment matrix and recolored by majority-overlap Yeo-7 network. (b) Warmer colors indicate greater AD-related loss of within-network co-grouping; the cyan circle marks the temporal pole.\", \"page\": 8, \"index\": 3, \"width\": 1073, \"height\": 447}]"
motivation: 现有方法忽视受试者间功能模块的个体差异，且未能利用发现的模块直接优化图连接模式。
method: 提出MPP-GNN，以双层优化实现层次化自适应图划分，捕获个体特异性模块并显式指导图学习。
result: 在两个AD分类数据集上达到最高AUC，模块发现与Yeo脑图谱高度一致，并揭示了AD的网络去分化现象。
conclusion: MPP-GNN通过自适应模块发现与图学习协同，有效提升了AD分类性能，并为脑网络病理研究提供了新视角。
---

## 摘要
功能磁共振成像（fMRI）是一种广泛用于研究大脑的技术。近年来，利用图神经网络（GNN）分析大脑功能连接的方法在脑部疾病分类（如阿尔茨海默病，AD）中展现出巨大潜力。然而，这些方法通常假设所有受试者具有预设数量的功能模块，忽视了受试者间的变异性。此外，发现的模块很少被用于直接指导学习到的连接模式。为解决这些问题，我们提出了一种元概率池化图神经网络（MPP-GNN）。我们将模型任务框架化为一个耦合的双层优化问题，通过层次化自适应图划分来发现个体特定的模块，然后将发现的大脑模块作为显式先验来指导边的精炼和表示学习。我们在两个公开的AD分类数据集上验证了MPP-GNN，与现有基线模型相比，在两个数据集上均取得了最高的AUC。此外，我们的分析表明，MPP-GNN与Yeo脑图谱定义的经典功能网络组织结构显著一致，并揭示了AD的网络水平去分化模式。

## Abstract
Functional magnetic resonance imaging (fMRI) is a widely used technique for studying the brain. Recent methods that utilize graph neural networks (GNNs) for analysis of brain functional connectivity have shown great potential for the classification of brain disorders, such as Alzheimer's disease (AD). However, these methods often assume a preset number of functional modules across all subjects, which overlooks inter-subject variability. In addition, the discovered modules are rarely used to directly guide the learned connectivity patterns. Here, to address these issues, we propose a Meta Probabilistic Pooling GNN (MPP-GNN). We frame the model's task as a coupled, bilevel optimization that performs adaptive graph partitioning hierarchically to discover subject-specific modules and then uses the discovered brain modules as an explicit prior to guide edge refinement and representation learning. We validate MPP-GNN on two public datasets for AD classification, achieving the highest AUC in comparison to established baselines for both datasets. Furthermore, our analysis demonstrates that MPP-GNN shows significant alignment with the canonical functional-network organization defined by the Yeo brain atlas and reveals a network-level dedifferentiation pattern for AD.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：本文与读者关注的“神经先验（neural prior）”和“fMRI表示（fMRI representation）”高度相关，它显式地将个体化脑网络社区结构作为先验引入图学习。
- **启发与意义**：将脑网络的模块性作为可学习的结构性先验，而非固定约束，这一思想对脑解码和表示对齐类研究有启发性，可推广至多视角约束下的个体化脑图谱构建。
- **可借鉴点**：双层优化（bilevel optimization）和AP-HPM的免预设聚类数目的分层池化设计，可以直接用于需要自适应捕获脑区功能模块的下游任务。
- **阅读建议**：重点阅读方法部分的AP-HPM和PERM设计，以及分析部分如何将模型发现与经典Yeo网络对齐，而非仅关注分类性能。

## 1. 论文的核心问题与整体含义
- **核心问题**：基于fMRI的脑功能连接分析中，现有多数图神经网络（GNN）方法假定所有受试者共享预定义的功能模块数量，忽略了受试者间的高度变异性；且已发现的模块很少被直接用来指导连接模式的学习。
- **整体含义**：提出一个将自适应社区发现、边降噪和图表示学习统一在单个端到端框架中的方法，用于阿尔茨海默病（AD）的分类，实现更鲁棒且更具神经生物学可解释性的模型。

## 2. 论文提出的方法论
- **整体框架**：元概率池化图神经网络（MPP-GNN），由外层元优化和内层图学习组成的双层优化结构。
- **外层（元优化）**：学习一个高斯分布 $\mathcal{N}(\mu, \sigma_{\text{meta}}^2 I)$ 来采样社区发现的元权重 $W_{\text{meta}}$，目标是最优化内层模型的验证性能，通过平滑变分优化（SBO）更新 $\mu,\sigma_{\text{meta}}$，使高分采样策略具有更高概率。
- **内层**：包括两个关键模块：
    - **基于亲和传播的层次池化（AP-HPM）**：利用采样的元特征和固定的拓扑特征（度差、最短路径）构建相似矩阵 $S^{(l)}$，通过亲和传播（Affinity Propagation）生成每层无预设簇数的社区分配矩阵 $Q^{(l)}$，然后平均池化节点特征和边权重，形成多层稀疏粗化图。该模块在每轮内层训练前预计算，保持结构固定。
    - **概率边精炼（PERM）**：以社区先验（是否同簇的指示 $C^{(l)}_{ij}$）、相似矩阵 $S^{(l)}_{ij}$和节点特征余弦相似度 $R^{(l)}_{ij}$ 为输入，使用可学习的标量参数化的逻辑门产生每条边的保留概率 $P^{(l)}$，再通过松弛伯努利采样生成掩码 $M^{(l)}$，得到精炼邻接矩阵 $\hat{A}^{(l)} = \tilde{A}^{(l)} \odot M^{(l)}$。
- **内层损失**：分类损失（焦点损失）加边概率的二值熵正则项 $L_{\text{edge}}$，促使边保留概率趋近0或1。
- **信息传递**：采用图卷积网络（GCN）进行消息传递，并使用GraphNorm和GELU激活。

## 3. 实验设计
- **数据集及场景**：
    - **UK Biobank**：300名受试者（54 AD，246 CN），用于二分类（AD vs CN）。
    - **ADNI**：680名受试者（68 AD，247 MCI，365 CN），用于三分类（AD vs MCI vs CN）。
- **预处理**：使用Schaefer 2018图谱定义419个ROI，节点特征经PCA降维至32维；边为偏相关系数，仅保留前30%连接。
- **基准对比方法**：
    - 传统机器学习：SVM、随机森林。
    - 通用GNN：GCN、GAT、GIN、GraphSAGE。
    - 通用图池化：DiffPool、TopKPool、SAGPool。
    - 脑网络专有模型：BrainNetTF、BrainNetCNN、BrainGNN、ContrastPool、IBGNN、Contrasformer。
- **评估指标**：AUC、准确率（ACC）、F1分数，均在5折交叉验证下报告均值和标准差。

## 4. 资源与算力
- 论文未明确提及使用的GPU型号、数量或具体训练时长。从方法复杂性推断，内层需对每个采样权重训练至收敛，共计50个元epoch、每个epoch 8个采样，运算量较大，但缺少具体算力数据。

## 5. 实验数量与充分性
- **主要实验**：两个公开数据集上的分类对比（Table II），共对比12种基线方法。
- **消融实验**：在UK Biobank上逐步加入AP-HPM、PERM和双层优化，以及损失函数消融，验证每个组件的贡献（Table III、IV）。
- **解释性分析**：在10个模型（5折UKB + 5折ADNI）上进行了边保留模式的可重复性分析和系统级差异分析；通过NMI与Yeo图谱比较验证社区发现的生物学合理性；展示了AD相关的网络去分化模式。
- **总体评价**：实验设计较为全面，覆盖了分类性能、模块解释性和消融验证，对比基准丰富且公平（相同数据划分），统计检验（Mann-Whitney U、FDR校正）增强客观性。

## 6. 论文的主要结论与发现
- MPP-GNN在UK Biobank和ADNI上均取得最高的AUC和F1，尤其在三分类ADNI上优势明显。
- AP-HPM发现的社区与Yeo经典功能网络显著一致（共识分区NMI超越99.7%的个体），且不依赖预设聚类数。
- PERM学习的边保留模式在不同数据划分与数据集间高度可重复。
- AD受试者的网络分区与Yeo网络的NMI显著低于CN，PERM显示感觉运动-默认网络、视觉-默认网络间边保留增高，而视觉-边缘系统降低，共同揭示网络级去分化模式。

## 7. 优点
- **方法新颖性**：首次将社区发现与边精炼作为耦合双层优化，实现模块先验指导图结构学习。
- **个体自适应**：AP-HPM无需预设簇数，自适应生成个体化分区，契合脑网络的异质性。
- **内建可解释性**：社区分配和门控系数直接提供模块归属和边重要性，无需事后归因方法。
- **生物学验证**：与金标准Yeo网络对齐，并发现符合已有文献的AD去分化模式，增强了模型的神经科学可信度。

## 8. 不足与局限
- **计算开销**：双层优化与内层每采样重训练导致计算成本高，文中未给出效率数据，可能限制大规模应用。
- **数据集样本量**：UK Biobank仅使用300名受试者（原始队列中AD比例极低），且ADNI三分类中MCI类别边界模糊，模型在该亚类上的性能未单独分析。
- **泛化性未测**：仅测试AD分类，没有在其他脑疾病（如自闭症、精神分裂症）数据集上验证通用性。
- **模块解释的粒度**：分析停留在系统级（Yeo-7/17），未深入单个ROI或细粒度环路，临床转化价值待进一步挖掘。
- **超参数敏感性**：AP偏好值、温度参数 $\tau$ 及各项权衡系数需调参，其敏感性缺乏讨论。

（完）
