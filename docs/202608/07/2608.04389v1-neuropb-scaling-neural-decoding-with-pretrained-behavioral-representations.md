---
title: "NeuroPB: Scaling Neural Decoding with Pretrained Behavioral Representations"
title_zh: "NeuroPB: 利用预训练行为表征扩展神经解码"
authors: "Luyao Jin, Yonghao Song, Huan Zhao, Vincent C. K. Cheung, Wei-Hsin Liao"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04389v1"
tags: ["query:fclip"]
score: 8.0
evidence: 将神经活动与预训练行为表征空间对齐
tldr: "为解决神经解码中神经数据有限的问题，本文提出NeuroPB框架，通过在大规模行为数据上预训练运动编码器，将神经活动对齐至行为表示空间，提升连续运动轨迹解码。在猕猴数据集上，行为预训练显著提升解码精度（R²提高11%和8%），且利用机器人轨迹预训练同样有效，显示了跨域迁移性。预训练规模越大，解码性能越好，并增强了跨会话、个体和任务的泛化，仅需少量校准数据即可匹配从头训练的性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04389v1/fig-001.webp\", \"caption\": \"Figure 1: Conceptual overview of NeuroPB. NeuroPB aligns neural activity with a behavioral representation pretrained on scalable macaque and robotic trajectories, enabling continuous trajectory decoding with generalization across recording sessions, subjects, and motor tasks.\", \"page\": 1, \"index\": 1, \"width\": 500, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-002.webp\", \"caption\": \"Figure 5: (A) Calibration-efficiency validation under crosssession, cross-subject, cross-task including CO-RT and RTCO. (B) Estimated calibration fraction required by each pretrained model to match the performance of Scratch using 100% of the data.\", \"page\": 6, \"index\": 2, \"width\": 500, \"height\": 583}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-003.webp\", \"caption\": \"Figure 2: Generalization performance is reported under cross-session, cross-subject, CO-RT, and RT-CO transfer settings. Bars represent the mean R2 obtained using RobotLarge-PT, Robot-Matched-PT, Macaque-PT, and Scratch, and error bars indicate the standard deviation. Statistical comparisons are performed between each pretrained condition and Scratch. ∗p < 0.05 and ∗∗p < 0.01.\", \"page\": 11, \"index\": 3, \"width\": 844, \"height\": 414}]"
motivation: 神经解码受限于神经记录规模小和异质，而行为数据易于大规模获取。
method: 先在大规模行为数据上预训练运动编码器，再用少量配对数据将神经活动对齐到行为表示空间，联合优化神经编码器和轻量解码器。
result: "在猕猴运动任务上，预训练将解码R²提高8-11%，机器人轨迹预训练效果相当，性能随预训练数据规模增加而提升，且仅需10%校准数据即可匹敌从头训练。"
conclusion: 行为预训练是神经解码的可扩展途径，为有限神经数据下高性能和校准高效的脑机接口提供了可行路径。
---

## 摘要
从神经活动中解码连续运动轨迹对于开发实用的脑机接口（BCI）至关重要。然而，当前的神经解码器受限于神经记录有限的规模与异质性。相比之下，行为数据可以更容易地从人类、动物、模拟和机器人系统中收集，且规模要大得多。在此，我们引入了 NeuroPB，一个通过从预训练的行为表征转移知识来扩展神经解码的框架。NeuroPB 首先在大规模运动行为数据上预训练一个运动编码器，然后使用有限的成对神经-行为记录，将神经活动与得到的行为表征空间对齐。随后优化神经编码器和轻量级运动解码器，从对齐的神经表征中重建连续运动。在多个猕猴运动数据集上，行为预训练改善了轨迹解码，与从头训练运动编码器相比，中心向外任务的 $R^2$ 提高了 11%，随机目标任务提高了 8%。值得注意的是，在机器人轨迹上的预训练达到了与猕猴轨迹预训练相当的性能，表明可迁移的运动学结构在生物和人工模型之间是共享的。此外，当神经数据量固定时，解码性能随着机器人预训练数据的规模和多样性的增加而提高。预训练还增强了跨记录会话、受试者和运动任务的泛化能力，仅需 10% 的校准数据即可匹配从头训练的性能。总体而言，这些结果确立了行为预训练作为神经解码的可扩展资源，并为在有限神经数据下实现高性能且校准高效的脑机接口提供了一条有前景的途径。

## Abstract
Decoding continuous motor trajectories from neural activity is essential for developing practical brain-computer interfaces (BCIs). However, current neural decoders are constrained by the limited scale and heterogeneity of neural recordings. In contrast, behavioral data can be collected more readily and at substantially larger scale from humans, animals, simulations, and robotic systems. Here, we introduce NeuroPB, a framework that scales neural decoding by transferring knowledge from pretrained behavioral representations. NeuroPB first pretrains a motor encoder on large-scale motor behavior data and then aligns neural activity with the resulting behavioral representation space using a limited set of paired neural-behavioral recordings. A neural encoder and lightweight motor decoder are subsequently optimized to reconstruct continuous movement from the aligned neural representations. Across multiple macaque motor datasets, behavioral pretraining improves trajectory decoding, including an 11% $R^2$ increase on center-out and 8% on random-target compared with training the motor encoder from scratch. Notably, pretraining on robotic trajectories achieves performance comparable to pretraining on macaque trajectories, demonstrating that transferable kinematic structure is shared across biological and artificial models. Moreover, decoding performance improves as the scale and diversity of robotic pretraining data increase, when the amount of neural data is fixed. Pretraining also enhances generalization across recording sessions, subjects, and motor tasks, with only 10% calibration needed to match training from scratch. Overall, these results establish behavioral pretraining as a scalable source for neural decoding and provide a promising route toward high-performance and calibration-efficient BCIs under limited neural data.

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议
- **关联方向**：本文与“representation alignment”和“neural prior”方向高度相关，利用大规模行为预训练表征作为神经解码的先验，实现跨模态表征对齐。
- **启发与意义**：行为数据易于获取且规模可扩展，其内在的运动结构可作为强大的先验，指导有限神经数据下的表征学习，为神经解码任务的泛化和少样本学习提供了新思路。
- **可借鉴点**：可借鉴基于对比学习的神经-行为表征对齐框架，将其扩展至fMRI、EEG等非侵入式脑信号与行为、语义等其他模态的对齐。
- **阅读建议**：建议重点关注行为预训练的数据规模效应、跨域迁移性的实验设计，以及校准效率验证方法，这些对于将行为先验引入其他脑解码范式具有借鉴意义。

## 1. 论文的核心问题与整体含义
- **核心问题**：连续运动神经解码对于脑机接口至关重要，但当前方法受限于神经记录规模小、异质性强，且神经数据收集昂贵而困难。能否利用易于大规模获取的行为数据（如动物、机器人轨迹）作为可迁移的先验知识，提升神经表征学习的效率和泛化性？
- **整体含义**：提出NeuroPB，通过从大规模行为数据预训练中迁移运动结构先验，将神经活动对齐到预训练的行为表征空间，从而在有限神经数据下实现高精度、校准高效且泛化性强的运动轨迹解码。

## 2. 论文提出的方法论
- **核心思想**：三阶段流水线——先在大规模行为数据上预训练运动编码器以获得结构化的行为表征；再用对比学习将神经活动与冻结的行为表征对齐；最后用轻量解码器从对齐的神经表征重建运动轨迹。
- **关键技术细节**：
  - **运动编码器**：基于Transformer，使用掩码轨迹重建进行预训练。输入为轨迹片段 $\mathbf{P} = [p_1,\dots,p_T]$，随机掩码部分时间步，学习预测被掩码的坐标，损失 $L_{\text{traj}} = \frac{1}{|\mathcal{M}|}\sum_{t\in\mathcal{M}}\|\hat{p}_t - p_t\|_2^2$。输出经池化并归一化得到行为表征 $z_m$。
  - **神经编码器**：基于Perceiver架构，处理时间戳标记的脉冲事件序列。通过跨注意力将变长脉冲序列压缩为固定数量的潜在token，经过多层自注意力，再池化和投影得到神经表征 $z_n$。
  - **模态对齐**：CLIP风格对称对比损失，最大化配对神经-行为表征相似度，最小化非配对相似度，$L_{\text{align}} = \frac{1}{2}(L_{n\to m} + L_{m\to n}) $，其中 $s_{ij} = (z_n^i)^\top z_m^j / \tau$。
  - **运动解码器**：从对齐后的神经表征 $z_n^i$ 通过MLP直接预测轨迹 $\hat{\mathbf{P}}_i$，损失为MSE。总损失 $L = L_{\text{align}} + L_{\text{motor}}$。
  - **预训练变体**：Scratch（随机初始化）、Macaque-PT（猕猴轨迹预训练）、Robot-Matched-PT（规模匹配机器人轨迹）、Robot-Large-PT（大规模机器人轨迹）。

## 3. 实验设计
- **数据集**：四个猕猴的M1/PMd阵列记录，包含中心向外（CO）和随机目标（RT）两个二维伸手运动任务。
- **Benchmark与对比方法**：Wiener Filter, Smoothing, DenseNN, RNN, LFADS, NEDS, NDT2, POYO。评价指标为轨迹预测的$R^2$。
- **实验场景**：
  - 单会话解码对比。
  - 行为预训练效果（Scratch vs. Robot-Large-PT）。
  - 预训练数据和规模的影响（Macaque-PT, Robot-Matched-PT, Robot-Large-PT对比）。
  - 校准效率验证：跨会话、跨受试者、跨任务迁移，使用1%-100%的校准数据。
  - 消融实验：去除对比损失、单元编码、RoPE、交叉注意力、自注意力。
  - 可视化分析：表征相似度矩阵和PCA。

## 4. 资源与算力
- 文中明确提到使用“RTX 4090 GPU”进行实现，但未提供GPU数量、训练总时长或总运算量等详细算力信息。其余训练配置（批次大小128，单周期余弦学习率调度等）在附录中给出，但算力成本评估不够完整。

## 5. 实验数量与充分性
- **实验组数**：约6大类实验（基线与SOTA对比、预训练效果对比、缩放预训练对比、4种校准效率曲线、5项消融、可视化分析），每类含多个子条件，总体实验设计较全面。
- **充分性与公平性**：对比方法覆盖经典到最新模型，消融系统性地验证了各组件贡献，跨会话/受试者/任务泛化实验增强了结论可靠性。校准比例实验设计严谨，但部分基线结果直接引用文献，未完全在同条件下复现，可能引入系统性差异。

## 6. 论文的主要结论与发现
- 行为预训练（Robot-Large-PT）相比从头训练，在CO和RT任务上分别提升$R^2$ 11%和8%。
- 猕猴轨迹和等量机器人轨迹预训练效果相当，表明运动结构可跨域迁移。
- 解码性能随机器人预训练数据规模和多样性增加而提升。
- 预训练显著增强跨会话、受试者、任务的泛化能力，仅需约10%校准数据即可达到Scratch全量校准的性能。
- 对齐后的神经表征呈现出按运动方向组织、与记录会话无关的分布。

## 7. 优点
- **创新性**：提出从行为侧入手，利用可扩展的行为先验来解决神经数据的稀缺性问题，思路新颖。
- **跨域迁移**：验证了机器人轨迹与生物运动之间的可迁移运动学结构，为使用合成数据辅助神经解码开辟了可能。
- **校准高效**：在泛化性实验中展现出极低的校准数据需求，具有实际应用潜力。
- **方法系统**：三阶段设计清晰，对比学习和Transformer的结合可复现性强，且推理时无需行为输入，部署便捷。

## 8. 不足与局限
- **评估局限**：仅在离线、侵入式猕猴记录上验证，未在人类、非侵入信号（如fMRI/EEG）或在线闭环BCI中测试。
- **任务范围**：解码任务限于二维平面伸手，未涉及更复杂的三维运动或涉及更多自由度的操作。
- **基线复现问题**：部分基线（NEDS, NDT2, POYO）的分数直接引用自其他文献，可能因实验设置不同而影响对比公平性。
- **算力报告缺失**：未报告预训练和整体训练的GPU时或碳排放，可复现性有限。
- **行为表征抽象程度**：机器人轨迹追踪的点轨迹与生物关节运动之间存在本质差异，其表征迁移的深层机制未作进一步分析。

## 9. 研究价值与阅读建议
- 详见开头“研究价值与阅读建议”部分。

（完）
