---
title: "Self-Geometry: GT-Free and Plug-and-Play Test-Time Adaptation for Geometrically Consistent 3D Vision Foundation Models"
title_zh: Self-Geometry：面向几何一致的3D视觉基础模型的无真值即插即用测试时自适应
authors: "Seokhyun Youn, Dahyeon Kye, Sung-Ho Bae, Jihyong Oh"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10708v1"
tags: ["query:fclip"]
score: 8.0
evidence: 三维视觉基础模型的多视图几何一致性约束
tldr: 3D视觉基础模型单次前向预测深度、姿态和点图，但预训练未施加显式多视图几何约束，导致不一致。本文提出Self-Geometry，一种免真值、即插即用的测试时自适应方法，利用2D像素对应作为伪真值，通过几何解耦优化（结合多视图一致性与极线一致性损失并梯度解耦）、基于SO(3)测地距离的视图采样和LoRA轻量适配，在多个模型与基准上一致提升姿态和几何估计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10708v1/fig-001.webp\", \"caption\": \"Fig. 2. Comparison of pretrained DA3-Base [3] with two test-time adaptation methods on a HiRoom [3] (828786) scene, decomposed along camera pose error, depth error, and the resulting pointmap. +Free-Geometry [12], a representative implicit self-consistency method, fails to substantially improve both the camera pose and depth, and consequently offers only limited improvement in the resulting pointmap. In contrast, +Self-Geometry (Ours), which directly imposes explicit multi-view geometric constraints, yields consistent improvements across all three. In the depth error maps and pointmaps, Red regions denote prediction errors exceeding the benchmark threshold and gray regions denote correctly reconstructed pixels.\", \"page\": 2, \"index\": 1, \"width\": 1072, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2608.10708v1/fig-005.webp\", \"caption\": \"Fig. 3. Overview of Our Proposed Self-Geometry Pipeline. Self-Geometry adapts a frozen pretrained VFM to a target scene through three complementary components. (i) GDO (Sec. III-B) formulates two GT-free correspondence-guided losses, the point-to-point MVC Loss (Eq. (2)) and the point-to-line EC Loss (Eq. (3)), and applies GD (Eq. (4)) to prevent the gradient conflict that can arise between them. (ii) FAN (Sec. III-C) samples angularly diverse views via scene-scale-invariant SO(3) geodesic distances (Eq. (5)). (iii) Lightweight TTA (Sec. III-D) updates only LoRA (Sec. III-D) parameters inserted into the attention blocks of the pretrained VFM. Together, these components operate as a GT-free and Plug-and-play TTA that imposes explicit multi-view geometric constraints on pretrained VFMs.\", \"page\": 4, \"index\": 5, \"width\": 1070, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2608.10708v1/fig-002.webp\", \"caption\": \"TABLE III Geometry Estimation results: F1-score (↑) under unposed (w/o p.) and posed (w/ p.) modes on 7Scenes [4], ETH3D [5], ScanNet++ [6], HiRoom [3], and Mean. Row notation: DA3-G/L/B/S denote DA3-Giant/Large/Base/Small [3]; each pretrained model (e.g., VGGT, π3, DA3-G) appears in four consecutive rows — the baseline and three adapted variants with suffix “-TCO” (TCO [36]), “-Fr” (Free-Geometry [12]), and “-Ours” (our proposed Self-Geometry). ∆% vs. baseline in parentheses. Per column, blue/red marks the largest gain/degradation among the three adaptation methods.\", \"page\": 7, \"index\": 2, \"width\": 1072, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2608.10708v1/fig-003.webp\", \"caption\": \"TABLE II Pose Estimation results: AUC@3, AUC@30 (↑) on 7Scenes [4], ETH3D [5], ScanNet++ [6], HiRoom [3], and Mean. Row notation: DA3-G/L/B/S denote DA3-Giant/Large/Base/Small [3]; each pretrained model (e.g., VGGT, π3, DA3-G) appears in four consecutive rows — the baseline and three adapted variants with suffix “-TCO” (TCO [36]), “-Fr” (Free-Geometry [12]), and “-Ours” (our proposed Self-Geometry). ∆% vs. baseline in parentheses. Per column, blue/red marks the largest gain/degradation among the three adaptation methods.\", \"page\": 7, \"index\": 3, \"width\": 1072, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2608.10708v1/fig-004.webp\", \"caption\": \"Fig. 4. Qualitative comparison of pretrained VFMs against our proposed Self-Geometry across representative scenes (rows: VGGT [1], π3 [2], DA3-Giant [3]; scene identifier below each row). (a) Depth Estimation: from left to right, Reference RGB, GT depth, and depth error maps for Original, +TCO [36], +Free-Geometry [12], and +Self-Geometry (Ours). (b) Geometry Estimation: from left to right, GT fused pointcloud and geometry error maps for the same four methods. Red regions denote errors exceeding the benchmark threshold; gray regions denote correctly reconstructed pixels. Best viewed in zoom.\", \"page\": 8, \"index\": 4, \"width\": 1072, \"height\": 352}]"
motivation: 预训练视觉基础模型缺乏显式多视图几何一致性，且现有隐式自一致性测试时策略提升有限，尤其在模型预测不准确时。
method: 提出Self-Geometry，包含几何解耦优化、Frame Angular-Neighbor视图采样和轻量LoRA适配，在测试时直接施加显式多视图几何约束。
result: 在六个视觉基础模型和四个基准上一致提升姿态和几何估计性能。
conclusion: Self-Geometry无需真值、即插即用，有效增强3D基础模型的几何一致性，具有广泛适用性。
---

## 摘要
近期的视觉基础模型（VFM）可在单次前向传播中预测深度、相机位姿和点图，无需逐场景优化，展现出强大的泛化能力。然而，施加显式多视图几何一致性（例如通过光束法平差）计算代价高昂，因此在VFM预训练期间未被采用，从而可能出现这类不一致。为了解决这一问题，先前工作在测试时施加从模型输出（如点图、特征）推导的隐式自一致性，但其性能提升固有地受限，尤其是在预训练VFM高度不准确的场景中。与这种隐式信号不同，我们提出Self-Geometry，一个即插即用的测试时自适应流程，它利用2D像素对应作为伪真值直接施加显式多视图几何约束。我们提出的Self-Geometry包括：几何解耦优化，将多视图一致性和极线一致性损失与梯度解耦相结合以防止梯度冲突；帧角度邻居，一种基于SO(3)测地距离的视图采样器，用于轻量地施加这些约束；以及轻量级TTA，通过LoRA自适应VFM。我们的方法在六个VFM（VGGT、π³、DA3-Giant/Large/Base/Small）和四个基准（7Scenes、ETH3D、ScanNet++、HiRoom）上，在姿态和几何估计方面均取得了一致的改进。

## Abstract
Recent Vision Foundation Models (VFMs) predict depth, camera pose, and pointmap in a single forward pass without per-scene optimization, achieving strong generalization. However, enforcing explicit multi-view geometric consistency, e.g., through bundle adjustment, is computationally costly and is thus not imposed during VFM pretraining, so such inconsistency can arise. To address this, implicit self-consistency derived from model outputs (e.g., pointmaps, features), though enforced at test-time in prior work, delivers inherently limited performance gain, especially on scenes where the pretrained VFM is highly inaccurate. In contrast to this implicit signal, we propose Self-Geometry, a plug-and-play test-time adaptation pipeline that directly imposes explicit multi-view geometric constraints using 2D pixel correspondences as pseudo ground-truth. Our proposed Self-Geometry consists of Geometric Disentanglement Optimization, which combines Multi-View Consistency and Epipolar Consistency losses with Gradient Disentanglement to prevent gradient conflict; Frame Angular-Neighbor, a view sampler based on SO(3) geodesic distances for lightly imposing these constraints; and Lightweight TTA, which adapts VFMs via LoRA. Our method achieves consistent improvements in both pose and geometry estimation across six VFMs (VGGT, $π^3$, DA3-Giant/Large/Base/Small) and four benchmarks (7Scenes, ETH3D, ScanNet++, HiRoom).

---

## 论文详细总结（自动生成）

## 研究价值与阅读建议

- **关联方向**：与读者“brain decoding / fMRI representation / brain encoding”方向**弱相关**；仅在“multi-view constraint / representation alignment”等术语层面存在间接联系。
- **启发与意义**：本文展示如何在没有外部真值的情况下，以显式几何一致性约束提升模型预测的一致性与表征质量；对脑信号解码中利用多视图/多模态一致性、无监督对齐有一定迁移启发。
- **可借鉴点**：几何解耦优化中的梯度解耦、基于 SO(3) 测地距离的视图采样、LoRA 轻量适配，可参考用于跨被试/跨视图脑表征对齐或轻量微调。
- **阅读建议**：若关注几何一致性约束或无真值适配，可精读方法部分；若专注 fMRI 表征解码，建议重点阅读其“伪真值一致性损失 + 轻量适配”思想，而无需深究 3D 视觉细节。

## 1. 论文的核心问题与整体含义

- **研究背景**：现代 3D 视觉基础模型（VFM，如 VGGT、π³、DA3 系列）能够在单次前向传播中预测深度、相机位姿和点图，无需逐场景优化，泛化能力强。
- **核心问题**：预训练过程中通常没有显式施加多视图几何一致性，因为类似光束法平差（bundle adjustment）的约束计算代价过高；因此模型输出可能存在几何不一致。
- **现有方法局限**：先前的测试时自一致性方法主要从模型输出（如点图、特征）推导隐式约束。这种隐式信号在预训练模型本来就高度不准确的场景中，提升幅度天然受限。
- **整体含义**：本文提出 **Self-Geometry**，在测试时利用 2D 像素对应作为伪真值，直接施加显式多视图几何约束，以即插即用、免真值的方式增强预训练 VFM 的几何一致性。

## 2. 论文提出的方法论

- **总体思想**：冻结预训练 VFM，在目标场景上通过测试时自适应（TTA）注入显式多视图几何约束；利用 2D 像素对应作为伪真值，不依赖真实深度或位姿标签。
- **三个核心组件**：
  - **几何解耦优化（GDO）**：
    - 构造两种无真值、以 2D 对应为指导的损失：
      - 点对点 **多视图一致性损失** $\mathcal{L}_{\text{MVC}}$（对应原文 Eq. (2)）。
      - 点对线 **极线一致性损失** $\mathcal{L}_{\text{EC}}$（对应原文 Eq. (3)）。
    - 使用 **梯度解耦（GD）**（对应原文 Eq. (4)）阻止两个损失之间的梯度冲突，避免多约束相互干扰。
  - **Frame Angular-Neighbor（FAN）视图采样**：
    - 基于 **SO(3) 测地距离**（对应原文 Eq. (5)）采样角度差异较大的视图。
    - 该采样方式具有场景尺度不变性，能够以轻量方式选择用于施加几何约束的视图对。
  - **轻量级 TTA（Lightweight TTA）**：
    - 仅更新插入到预训练 VFM 注意力模块中的 **LoRA** 参数，而不更新原始主干网络。
    - 保持即插即用和较低计算代价。
- **整体流程**：对目标场景，先通过 FAN 选取合适视图，再用 2D 像素对应构造 MVC 与 EC 损失，在冻结主干的前提下仅优化 LoRA 参数，从而提升深度、位姿和点图一致性。

## 3. 实验设计

- **数据集 / 场景**：
  - 7Scenes
  - ETH3D
  - ScanNet++
  - HiRoom
- **Benchmark 指标**：
  - 位姿估计：AUC@3、AUC@30（越高越好）。
  - 几何估计：F1-score（↑），分别报告 unposed（w/o p.）与 posed（w/ p.）两种模式。
  - 定性评估：深度误差图、几何误差图、点图/点云重建。
- **对比方法**：
  - 预训练 VFM 基线：VGGT、π³、DA3-Giant / Large / Base / Small，共 6 个模型。
  - 三种测试时自适应变体：
    - `-TCO`：TCO [36]
    - `-Fr`：Free-Geometry [12]，代表性隐式自一致性方法
    - `-Ours`：Self-Geometry
- **结果呈现**：
  - Table II：位姿估计结果。
  - Table III：几何估计 F1 结果。
  - Fig. 2：HiRoom 场景上 DA3-Base 的位姿、深度和点图误差对比。
  - Fig. 4：多模型多场景的定性与几何重建对比。

## 4. 资源与算力

- 从提供的论文文本中，**未明确说明 GPU 型号、GPU 数量、训练/自适应时长或显存占用**。
- 因此无法判断该方法的真实计算开销与可复现性；需要结合正文完整版本或补充材料确认。
- 论文强调“轻量 TTA”和“LoRA”，预期相比全参数微调计算成本较低，但缺少具体数值支持。

## 5. 实验数量与充分性

- **实验规模**：至少覆盖 6 个 VFM × 4 个基准 × 4 个方法变体（1 个基线 + 3 种 TTA），并在位姿与几何两个任务上报告指标。
- **充分性**：
  - 跨模型数量较多，包含 DA3 的 Giant / Large / Base / Small 多个规模，能验证方法对不同模型容量的适用性。
  - 跨基准覆盖 4 个数据集，能体现场景泛化性。
  - 定量与定性实验均有，图表演示比较直观。
- **不足与公平性**：
  - 提供的文本中未见详细消融实验表，无法确认 GDO、FAN、LoRA 各组件单独的增益幅度。
  - 未见多次随机种子或统计显著性检验，结果稳健性难以从当前摘要判断。
  - 对比的 TTA 方法仅有 TCO 与 Free-Geometry 两类，未包含监督式场景微调或其他无真值 TTA 基线。
  - 缺少 2D 像素对应来源、匹配质量、超参数敏感性等分析。
- **客观性**：在免真值 TTA 设定下，与 TCO、Free-Geometry 的对比相对公平；但缺乏更强监督上界和更全面的消融，使结论仍有补充空间。

## 6. 论文的主要结论与发现

- 预训练 VFM 没有显式多视图几何约束，因此会产生位姿、深度和点图不一致。
- 仅依靠隐式自一致性的 Free-Geometry 在模型预测不准确时提升有限；而显式 2D 像素对应伪真值约束能够带来更一致提升。
- Self-Geometry 在 6 个 VFM 和 4 个基准上均改善了位姿与几何估计。
- 梯度解耦能缓解 MVC 与 EC 损失之间的冲突，SO(3) 测地距离采样与 LoRA 轻量适配共同支撑了无真值、即插即用的测试时自适应框架。

## 7. 优点

- **免真值、即插即用**：不需要场景深度或位姿标签，适合部署到不同预训练 VFM 上。
- **显式几何约束**：相比从模型输出推导的隐式自一致性，更具信息量和校正能力。
- **梯度解耦设计**：减少多损失之间的优化冲突。
- **轻量适配**：仅更新 LoRA 参数，冻结主干，降低计算与存储成本。
- **验证广泛**：跨模型规模、跨多个室内/场景基准，结果具有较好的通用性。

## 8. 不足与局限

- **算力信息缺失**：没有披露 GPU 型号、数量、运行时间等，难以评估真实部署成本。
- **场景覆盖有限**：所用数据集以室内、静态场景为主，缺少室外、动态、大范围或时序变化场景验证。
- **依赖 2D 对应质量**：伪真值约束依赖像素匹配，若对应错误或稀疏，可能误导优化。
- **消融不充分**：从现有摘要看，缺少各组件的单独消融和超参数敏感性分析，难以判断每部分贡献。
- **对比方法有限**：只对比了少数免真值 TTA 方法，未与监督式场景微调或更多 TTA / 几何优化方法比较。
- **鲁棒性证据不足**：缺少统计显著性、多次运行方差和失败案例分析。

## 9. 主要贡献总结

- 提出 **Self-Geometry**：一个免真值、即插即用的测试时自适应框架，用于提升 3D VFM 的几何一致性。
- 设计 **几何解耦优化**、**Frame Angular-Neighbor 视图采样**、**轻量 LoRA TTA** 三个组件。
- 在多个 VFM 与多个基准上验证了姿态和几何估计的稳定提升，证明了显式多视图几何约束在测试时自

适应中的有效性。总体而言，Self-Geometry 通过显式 2D 对应伪真值、几何解耦优化与轻量 LoRA 适配，在免真值测试时设定下系统性地提升了多类 3D 视觉基础模型的几何一致性，体现了显式多视图约束在无外部监督条件下仍具有较强的校正能力。

（完）
